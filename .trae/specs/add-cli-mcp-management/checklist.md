# Checklist

- [x] `packages/protocol/schema/v2.json` 中 `minor` 为 5，`client_methods` 包含 `"mcp.status"`，`server_capabilities` 包含 `"mcp.read"`
- [x] `packages/protocol/src/generated.ts` 由生成脚本重新生成，包含 `McpServerStatus`、`McpStatusParams`、`McpStatusResult` 类型
- [x] `packages/protocol/src/index.ts` 导出 `Method.MCP_STATUS` 常量和 `assertMcpStatusParams` 断言
- [x] `packages/agent/harness_agent/protocol_generated.py` 由生成脚本重新生成，包含对应 Pydantic 模型
- [x] `McpConnectionManager` 新增 `get_server_statuses()` 方法，返回逐服务器状态（name、transport、status、error、tool_names）
- [x] 部分服务器连接失败时，成功服务器的工具仍正常可用，失败服务器标记为 `failed` 并附带错误信息
- [x] 环境变量缺失的服务器标记为 `skipped`，错误信息包含缺失变量名
- [x] `server.py` 的 `_handlers` 注册了 `"mcp.status"` 处理器
- [x] `_handle_mcp_status` 返回 `{"servers": [...], "total_tools": N}` 格式
- [x] 未初始化时调用 `mcp.status` 返回 `-32002` 错误
- [x] `packages/cli/src/tui/commands.ts` 中 `SlashCommandName` 包含 `"mcp"`，`slashCommandDefinitions` 包含 mcp 定义
- [x] `/mcp` 命令执行后在时间线渲染 MCP 状态面板（服务器名称、传输类型、状态颜色、工具列表、错误信息、汇总行）
- [x] 无 MCP 配置时 `/mcp` 显示"未配置"提示和配置引导
- [x] `mcp.status` 请求失败时 TUI 显示错误通知，不阻塞
- [x] `/status` 输出包含 MCP 摘要行（`MCP: N/M connected, T tools` 或 `MCP: not configured`）
- [x] `IpcClient` 新增 `mcpStatus()` 便捷方法
- [x] `commands.test.ts` 中 `findSlashCommands("/mcp")` 断言已更新为匹配 mcp 命令
- [x] `app-interaction.test.ts` 新增 `/mcp` 集成测试
- [x] `test_mcp.py` 新增逐服务器状态测试（全部连接、部分失败、全部跳过、空配置）
- [x] `docs/user/交互使用.md` 中 `/mcp` 已从"尚未接入"列表移除，并添加命令说明
- [x] `bun run typecheck` 通过
- [x] `bun run test` 通过（TypeScript 端）
- [x] `cd packages/agent && .venv/Scripts/python -m pytest -q` 通过（Python 端）
- [x] `bun run project:check` 通过
