# Plan: HC-159 下线后台任务假工具

> 依赖：Task HC-159、Spec HC-159。范围以 Spec 为准，本计划不新增范围。

## 概述

按 TDD 分两个停点完成：先让负向测试失败（红），再删除实现与接线使其通过（绿）；随后同步文档、跑全量检查并提交归档。

## 依赖顺序

1. 负向测试（红）→ 2. 源码删除（绿）→ 3. 文档与展示面 → 4. 全量验证与提交。
步骤 3 依赖步骤 2 完成后的最终名字集合，不可与步骤 2 并行。

## 步骤

### 停点 1：工具面与策略面删除（代码 + 测试）

- P1-1 写负向测试（红）：
  - test_harness_tools.py 新增断言：create_harness_tools() 名字集合不含三个工具；
  - test_deferred_tools.py 设计断言更新 + disjoint 断言；
  - test_subagents_tools.py ALL_TOOL_NAMES 负向断言；
  - cli tool-display-policy.test.ts 新增断言：toolDisplay("monitor").known === false。
  验证：focused pytest 非零退出，且失败项包含新增断言。
- P1-2 删除实现与接线（绿）：
  - 删除 harness_tools.py 三个注册块、import 与 task_manager 实例；删除 tools_mode.py 文件；删除 agent.py 三条工具契约；
  - 收敛策略/运行时面的名字（capability_policy、approval_policy、bash_matcher、tool_risk、rule_parser、permission_rules、concurrency、execution_stream、deferred_tools、subagents、run_coordinator）；
  - 删除 CLI 展示目录中的 monitor 条目；
  - 更新受影响的既有测试（删除 monitor/task_output/task_stop 相关用例与参数化行）；
  - 重命名 test_tools_mode_memory.py → test_tools_memory.py。
  验证：pytest focused 全绿；bun test 展示策略与渲染测试全绿。

**停点 1 演示**：用户在终端运行 focused 测试命令看到全绿；`grep -rn "task_output\|task_stop" packages/agent/harness_agent` 无命中。

### 停点 2：文档、全量检查、提交与归档

- P2-1 更新 docs/user/subagent.md（工具表 20→17、删三行）与 docs/user/模型配置.md（低频名单）；
- P2-2 全量验证：packages/agent 全量 pytest、packages/cli bun test、bun run project:check；
- P2-3 按包提交：feat(agent) 与 feat(cli)+docs 两个 commit；
- P2-4 看板归档：task:complete 记录证据，tasks:sync。

## 风险与缓解

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| 策略常量被未发现的测试引用 | 测试失败 | 全量 pytest 兜底 |
| bun 测试环境不可用 | 停点 1 CLI 验证受阻 | 记录跳过原因，转全量检查 |
| 历史规则文件含 KillShell | 规则失效 | fail-closed 语义兜底，Task 已记录 |

## 验证命令

- focused：cd packages/agent && .venv/bin/python -m pytest -q tests/tools/test_harness_tools.py tests/tools/test_tools_memory.py tests/policy/test_tool_risk.py tests/policy/test_approval_policy.py tests/policy/test_rule_parser.py tests/policy/test_approval_pipeline.py tests/policy/test_permission_rules.py tests/runtime/test_deferred_tools.py tests/runtime/test_agent.py tests/runtime/test_subagents_tools.py
- CLI：cd packages/cli && bun test tests/presentation-shared/tool-display-policy.test.ts tests/presentation-shared/tool-output-render.test.ts
- 全量：cd packages/agent && .venv/bin/python -m pytest -q && cd ../cli && bun test && cd ../.. && bun run project:check
