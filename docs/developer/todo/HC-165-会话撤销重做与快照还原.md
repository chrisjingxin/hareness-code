# HC-165 会话撤销重做与快照还原 执行清单 (Todo)

## 关联文档

- 关联 Task：[HC-165-会话撤销重做与快照还原.md](../task/archive/HC-165-会话撤销重做与快照还原.md)
- 关联 Spec：[HC-165-会话撤销重做与快照还原.md](../spec/HC-165-会话撤销重做与快照还原.md)
- 关联 Plan：[HC-165-会话撤销重做与快照还原.md](../plan/HC-165-会话撤销重做与快照还原.md)

---

## 阶段一：协议与命令注册

### 目标与交付物
在 Protocol 层扩展 Undo/Redo 契约，并在 Command Registry 中注册 `/undo` 与 `/redo` 命令。

- [x] **1.1 Protocol 类型定义**
  - **动作**：在 `packages/protocol/src/` 中增加 `threads.list_turns`、`threads.undo`、`threads.redo` 参数与结果类型，在 Method 常量表中增加方法名。
  - **验收**：`bun run build` 编译通过，无类型报错。
  - **验证**：`cd packages/protocol && bun test`。

- [x] **1.2 Command Registry 注册与可用性规则**
  - **动作**：在 `packages/cli/src/interactive/commands.ts` 中注册 `thread.undo`（aliases: `rewind`, `rollback`）与 `thread.redo`；更新 `availability` 逻辑。
  - **验收**：`/undo` 在有可用 thread 时可见；`/redo` 在非暂存回退态时展示 disabled 及原因。
  - **验证**：`cd packages/cli && bun test tests/interactive/commands.test.ts`。

> **🎯 停点 1 验证方法**：
> 运行 `bun run dev`，在输入框键入 `/`，观察菜单中出现 `/undo`，键入 `/redo` 观察到禁用提示。

---

## 阶段二：Python 后端 Git Checkpoint 与快照还原

### 目标与交付物
在 Python 端实现隐式 Git 树快照录制、Turns 列表查询、Undo/Redo 状态机与物理清理。

- [x] **2.1 Git Checkpoint 录制服务**
  - **动作**：在 `packages/agent/harness_agent/threads/` 中实现 `GitCheckpointService`，在 Run 启动与结束时调用 `git write-tree` 记录当前树哈希。
  - **验收**：成功在内部数据库记录 `turn_id` 与 `tree_oid` 对应关系，不产生多余 commit。
  - **验证**：`cd packages/agent && .venv/bin/python -m pytest tests/threads/test_git_checkpoints.py`。
  - **证据**：2026-09-01 `tests/threads/test_git_checkpoints.py` 4 passed；快照与还原均使用隔离 index，HEAD / `git ls-files -s` 不变。

- [x] **2.2 RPC Handler 实现**
  - **动作**：在 `harness_agent/host/` 中实现 `threads.list_turns`、`threads.undo`、`threads.redo` 处理器，接入工作区还原与 Thread 消息游标回拨。
  - **验收**：`threads.undo` 成功根据模式还原文件并标记暂存态；新 Run 启动时自动触发物理 `cleanup`。
  - **验证**：`cd packages/agent && .venv/bin/python -m pytest tests/host/test_thread_undo_redo.py`。
  - **证据**：2026-09-01 `tests/host/test_thread_undo_redo.py` 6 passed；`list_turns` 使用 User record_id，`both`/`conversation`/`code` 三档与新 Run 物理截断均有断言。

> **🎯 停点 2 验证方法**：
> 运行 `cd packages/agent && .venv/bin/python -m pytest -q`，所有 Checkpoint 与 Undo/Redo 相关单测全部通过。

---

## 阶段三：CLI 核心交互状态机与 Dispatcher 联动

### 目标与交付物
在 TypeScript Interactive Core 中接入 Undo / Redo 语义动作与状态机。

- [x] **3.1 Dispatcher 语义映射**
  - **动作**：在 `packages/cli/src/interactive/command-dispatcher.ts` 中映射 `thread.undo` -> `present` 历史选择器；`thread.redo` -> `request-redo` 语义动作。
  - **验收**：Dispatcher 能够正确生成结构化 semantic operation。
  - **验证**：`cd packages/cli && bun test tests/interactive/commands.test.ts`。
  - **证据**：2026-09-01 `commands.test.ts` 27 passed；`/undo` 与 `/redo` 解析、可用性规则、`present undo` 与 `request-redo` 语义映射均全绿。

- [x] **3.2 Controller 状态管理与 RPC 驱动**
  - **动作**：在 `packages/cli/src/interactive/controller.ts` 中维护 `isReverted` 与 `revertedTurnId` 暂存回退态，处理 `thread.undo` 与 `thread.redo` intent，并在 `run.start` 时自动重置暂存态。
  - **验收**：Controller 能够在 Undo/Redo 成功后广播更新 Timeline、通知与上下文快照。
  - **验证**：`cd packages/cli && bun test tests/interactive/controller.test.ts`。
  - **证据**：2026-09-01 `controller.test.ts` 6 passed；`thread.undo` -> `thread.redo` -> `input.submit` 完整状态机与持久通知全绿。

> **🎯 停点 3 验证方法**：
> 运行 `cd packages/cli && bun test`，核心控制流与状态机测试全绿。

---

## 阶段四：TUI 交互浮层与全链路端到端集成

### 目标与交付物
在 OpenTUI 中实现回合选择器 `SearchPicker` 与二次确认 `DialogShell`。

- [x] **4.1 TUI UndoPicker 视图**
  - **动作**：在 `packages/cli/src/tui/presentation/` 中实现历史回合选择浮层，按时间倒序列出 User Turns、Prompt 摘要与变动文件数标签。
  - **验收**：支持键盘光标上下导航、高亮过滤与 Enter 选择。
  - **验证**：`cd packages/cli && bun test tests/tui/undo-picker.test.ts`。
  - **证据**：2026-09-01 `tests/tui/undo-picker.test.ts` 7 passed；`UndoPicker` 标题、输入过滤、diff 统计格式化与选择回调全部通过。

- [x] **4.2 TUI 二次确认与选项浮层**
  - **动作**：实现选项对话框，提供 `both` / `conversation` / `code` 键盘焦点单选，非 Git 仓库提示禁用。
  - **验收**：确认后触发 Controller 执行 Undo 并弹出 Toast 通知。
  - **验证**：`cd packages/cli && bun test tests/tui/undo-picker.test.ts`。
  - **证据**：2026-09-01 `tests/tui/undo-picker.test.ts` 7 passed；Git 与非 Git 仓库选项可用性、快捷键（1/2/3/Enter/Esc）与 `TuiAdapter` -> `Gateway` 全链路回退执行均验证通过。

> **🎯 停点 4 验证方法**：
> 启动 `bun run dev`，在多次问答与代码修改后，输入 `/undo` 挑选第 1 轮进行回退，观察代码与对话同步还原；输入 `/redo` 恢复；输入新 Prompt 开启新分支。
