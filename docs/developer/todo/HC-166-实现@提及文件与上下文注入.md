# HC-166：实现 @ 提及文件与上下文注入执行清单

> 原始需求：[HC-166](../task/archive/HC-166-实现@提及文件与上下文注入.md)  
> 实施计划：[HC-166 计划](../plan/HC-166-实现@提及文件与上下文注入.md)

---

## 阶段一：WP1 - 表现共享层词法解析与文件模糊匹配 (TDD)

- [x] 1.1 在 `packages/cli/tests/presentation-shared/mention-query-policy.test.ts` 中编写测试：
  - 验证光标在 `@` 紧邻右侧时激活触发（`searchQuery: ""`）；
  - 验证光标在 `@partial_path` 末尾时提取搜索词与起止 offset；
  - 验证光标在 `@"quoted partial"` 末尾时正确处理引号；
  - 验证邮箱（如 `user@domain.com`）或单词中间的 `@` 不误触发；
  - 验证普通文本无 `@` 时返回未激活。
- [x] 1.2 创建 `packages/cli/src/presentation-shared/mention-query-policy.ts`，实现光标处 `@` 触发解析纯函数。
- [x] 1.3 在 `packages/cli/tests/presentation-shared/mention-filter-policy.test.ts` 中编写测试：
  - 验证根据输入 query 过滤 `WorkspaceTreeRow` 文件列表；
  - 验证目录项被排除（仅保留 `kind !== "directory"`）；
  - 验证匹配项按相关度排序并截断至 Top 15。
- [x] 1.4 创建 `packages/cli/src/presentation-shared/mention-filter-policy.ts`，实现候选过滤与排序纯函数。
- [x] 1.5 运行 `cd packages/cli && bun test tests/presentation-shared/mention-query-policy.test.ts tests/presentation-shared/mention-filter-policy.test.ts`，确保测试通过。

---

## 阶段二：WP2 - TUI 输入栏 @ 补全菜单与键盘回填交互

- [x] 2.1 创建 `packages/cli/src/tui/presentation/mention-menu.tsx`（或在 `input-bar.tsx` 中实现 `<MentionMenu />` 组件）：
  - 渲染简洁的纯相对路径；
  - 采用与 `<CommandMenu />` 一致的 `tuiTheme.surface` 背景与悬浮定位。
- [x] 2.2 在 `packages/cli/src/tui/application/adapter.ts` 扩展 `MentionMenuState`：
  - 在 `updateDraft` 中调用 `mention-query-policy` 检测 `@` 触发，若触发则通过 `WorkspaceExplorer` 快照筛选文件候选；
  - 实现 `mention-menu-select` 意图，将选中路径替换光标处的 `@query`（含空格路径自动加引号 `@"path"`，末尾追加空格，光标移至末尾）；
  - 实现键盘 `↑`/`↓`（或 `k`/`j`）选择、`Tab`/`Enter` 选中回填、`Esc` 退出补全。
- [x] 2.3 在 `packages/cli/src/tui/app.tsx` 中接入 `mentionMenu` 键盘事件分发与组件挂载。
- [x] 2.4 在 `packages/cli/tests/tui/adapter-mention.test.ts` 编写 TUI Adapter 状态流转与补全测试。
- [x] 2.5 运行 `cd packages/cli && bun test tests/tui/adapter-mention.test.ts` 与 `bun run typecheck`。

> **可演示停点 1（Checkpoint 1）**：  
> 运行 `bun run dev`，在输入框中键入 `@` 或 `@app`，观察输入框上方即时弹出纯路径工作区文件列表；使用方向键选择并按 `Tab` 或 `Enter`，路径自动回填上屏（带空格自动包裹引号并追加空格）。

---

## 阶段三：WP3 - 提及安全提取与 128k 保守门禁解析器 (TDD)

- [x] 3.1 在 `packages/cli/tests/presentation-shared/mention-parse-policy.test.ts` 编写提取测试：
  - 提取 `@path/to/file.ts`；
  - 提取 `@"docs/my file with spaces.md"`；
  - 提取 `@src/index.ts#L20-50`（切片起止行）；
  - 提取 `@src/app.tsx#L10`（单行切片）。
- [x] 3.2 创建 `packages/cli/src/presentation-shared/mention-parse-policy.ts`，实现提及正则与行号解析纯函数。
- [x] 3.3 在 `packages/cli/tests/workspace/mention-resolver.test.ts` 编写内容读取与预算门禁测试：
  - 小文件（< 10KB）正常生成包含完整代码的 `[Attached Context: ...]`；
  - `#L20-50` 行切片精确提取指定行内容并标注行号区间；
  - 超大文件（>= 10KB）自动降级为 `[Mentioned File: ... too large to inline]` 占位；
  - 黑名单文件（`.lock`、`.min.js`、`.map`、图片等）自动降级为占位；
  - 多文件总预算超过 20KB 时自动截断并转为占位；
  - `../../` 越界路径通过 `resolveWithinRoot` 被安全忽略。
- [x] 3.4 创建 `packages/cli/src/workspace/mention-resolver.ts`，实现文件安全读取、切片与门禁组装。
- [x] 3.5 运行 `cd packages/cli && bun test tests/presentation-shared/mention-parse-policy.test.ts tests/workspace/mention-resolver.test.ts`。

> **可演示停点 2（Checkpoint 2）**：  
> 运行上述 focused tests，确认普通路径、引号路径、行号切片、10KB/300 行边界、20KB 总预算、二进制降级与越界防御均通过。

---

## 阶段四：WP4 - 端到端 Prompt 注入与 Agent 即时感知闭环

- [x] 4.1 在 `packages/cli/src/interactive/controller.ts` 的 `handleSubmit`（或 `runFeature.startRun`）流程中：
  - 调用 `mentionResolver.resolveMentions(message)`；
  - 将生成的 `contextBlock` 前置拼装到实际发给 Agent 的 Prompt 中；
  - 确保 Draft 清空与 UI Timeline 仍保持用户输入的原始消息（含 `@file`），不污染用户视觉历史。
- [x] 4.2 在 `packages/cli/tests/interactive/controller-mention.test.ts` 中编写端到端提交流程测试。
- [x] 4.3 运行全量包检查：`cd packages/cli && bun test` 与 `bun run typecheck`。

> **可演示停点 3（Checkpoint 3）**：  
> 运行 `bun run dev`，在输入框中输入 `@packages/cli/src/index.ts#L1-20 请解释这几行代码`，观察 Agent 在首轮直接基于内联的代码进行精准回答，无需任何多余的 `read_file` tool call。

---

## 阶段五：WP5 - TUI 与 Web 大文件集候选菜单（停点 4，TDD）

- [x] 5.1 先扩充共享层失败测试：搜索结果保留真实匹配总数、最多 1000 项、截断标记与匹配范围；窗口导航覆盖 8/5 行、PageUp/PageDown 和首尾夹取。
- [x] 5.2 最小实现共享 `MentionSearchResult` 与窗口导航纯函数，并导出给 TUI/Web 使用。
- [x] 5.3 先扩充 TUI 失败测试，再实现 8/5 行窗口、匹配高亮、长路径省略、计数/截断/加载/空状态和键盘翻页/滚动。
- [x] 5.4 先扩充 Web adapter/composer 失败测试，再实现 @ 状态机、8 行菜单、键盘/鼠标导航与选中回填。
- [x] 5.5 扩展内部 Web UI contract 以携带完整工作区条目；停点 5 将 canonical 字段收敛为 `WorkspaceTreeState.allEntries`，覆盖文件搜索与目录浏览并提升 `UI_CONTRACT_VERSION`。
- [x] 5.6 运行停点 4 focused tests、CLI 包级测试与 `bun run typecheck`；focused tests `164 pass`，CLI 全量 `1046 pass / 1 skip`（另有 11 项宿主权限失败），typecheck、build、project:check 与 diff check 均通过。

> **可演示停点 4（Checkpoint 4）**：  
> 分别运行 TUI 与 Web UI，在文件很多的仓库输入 `@app`；两端固定显示一页候选，方向键、PageUp/PageDown 与鼠标滚动可浏览，底部显示完整匹配数，长路径被省略且匹配字符高亮。完成后停止，等待用户验收。

---

## 阶段六：WP6 - TUI 与 Web 目录下钻（停点 5，TDD）

- [x] 6.1 先写共享层失败测试：空查询只返回当前目录直接子项，目录优先且排序稳定。
- [x] 6.2 先写双端状态机失败测试：从根目录进入子目录、返回上级、目录不回填、文件正常回填。
- [x] 6.3 最小实现共享目录浏览状态与双端键盘/鼠标交互；目录追加 `/`，底部显示浏览路径，空查询下 `Right`/`Tab`/`Enter` 进入、`Left` 返回。
- [x] 6.4 运行双端 focused tests、CLI 包级测试与 `bun run typecheck`；focused tests `198 pass`，CLI 全量 `1054 pass / 1 skip`（另有 11 项既有宿主权限失败），typecheck、build、project:check 与 diff check 均通过。

> **可演示停点 5（Checkpoint 5）**：  
> 在 TUI 与 Web 输入单独的 `@`，从根目录逐层进入 `packages/cli/src`，选中文件后准确回填；完成后停止，等待用户验收。

---

## 阶段七：WP7 - 文档更新、质量审查与归档准备

- [x] 7.1 更新用户文档 `docs/user/交互使用.md`，添加 `@` 文件提及、目录下钻、预算门禁与 `#L10-50` 行号切片说明。
- [x] 7.2 检视并增量更新 `docs/developer/architecture/架构总览.md`，记录跨端菜单、单轮消息增强与安全边界。
- [x] 7.3 使用 `agent-skills:code-review-and-quality` 进行多轴代码审查；修复 3 项 Required：无界切片源读取、菜单关闭仍扫描工作区、Markdown 围栏提前闭合，结论写回 Task。
- [x] 7.4 完成最终验证：focused tests `213 pass`；CLI 全量 `1056 pass / 1 skip`，另有 11 项既有宿主权限失败；typecheck、build、project:check 与 diff check 均通过。
