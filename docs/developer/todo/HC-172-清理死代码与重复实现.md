# HC-172 清理死代码与重复实现执行清单

关联：[Task](../task/HC-172-清理死代码与重复实现.md) · [Spec](../spec/HC-172-清理死代码与重复实现.md) · [Plan](../plan/HC-172-清理死代码与重复实现.md)

每项必须先写最小失败测试（或负向 `rg` / 编译失败断言），再写实现。一次只做到下一个可演示停点；完成本段后勾选、记录实际命令和结果、覆盖更新 `tmp/handoff.md`，然后停下等用户看过。未经用户要求不得继续下一停点。

**冻结清单不是待办。** 「本任务明确不做」一节禁止改成 `[x]`，禁止在本编号的 diff 里实现那些项。发现范围膨胀必须停写，回 Task 阻塞点。

---

## 本任务明确不做（冻结）

这些项在 2026-09-10 的需求确认中被明确排除。它们可以留在仓库，由归属 Task 或后续另开 Task 处理。**本 Todo 的停点 1 / 停点 2 都不得改下列路径上的产品行为或「顺手重构」。**

| 未做事项 | 归属 | 为什么本任务不做 |
| --- | --- | --- |
| 删除 `ComposeWorkItemEngine` 及 `compose/activities/*`、`engine_services.py`、`guard.py`、`readiness.py`、`turn_intent.py`、`document_store.py`、`threads/compose_work_item_store.py`、`compose_activity_store.py` | [HC-155](../task/HC-155-重做Compose流程.md)（待验收，scope 已含「删除 Work Item 引擎」） | 现任 Task 欠债，另开一份会抢同一段代码 |
| Host 仍组装完整 `EngineDriverServices`（stage agent + VerificationPort），Adapter 只用 `workspace_root` | HC-155 | 与 Work Item 引擎同一包袱 |
| `DiagnoseDriver`、两份 `VerificationPort`、`SideAnswerPort` | HC-155 | 挂在 Work Item 活动上 |
| 删除或接线 TUI `file-icons.ts` | [HC-152](../task/HC-152-TUI文件树语言图标体系.md)（待验收） | 可能是没接到文件树，不是已确认的死代码 |
| legacy 迁移 child / poison / attempt manifest；拆 `thread_persistence.py`（10085 行） | [HC-108](../task/HC-108-收敛迁移子进程强制清理与残留文.md)（进行中） | 同一文件，禁止并行改 |
| `LegacyModelBindings` / `snapshot_from_legacy_prompt_epoch` 双路径 | HC-108 及持久化迁移 | 数据迁移，不是无主别名 |
| 拆 `AgentHost`（~5772 行） | 后续另开 Task | 搬家不等于变短，且与多项进行中 Host 工作撞车 |
| 拆 `settings.py`（~3962 行） | 后续另开 Task | 同上 |
| 拆 TUI/Web `adapter.ts`（~2114 / ~1580 行） | 后续另开 Task；并避开 HC-145 / HC-150 / HC-151 | 进行中的表现层任务在改这些文件 |
| 收薄 `RunCoordinator` → `BuildRunAdapter` → `ManagedAgentExecutor` → `execution_stream` 四层 | 后续另开 Task | 活路径重构，不是删除/合并无主代码 |
| 绑定名堆：`ThreadExecutionSelection` / `ResolvedExecutionBinding` / `RunExecutionBinding` / `AgentExecutionBinding` | 后续另开 Task | 领域命名收敛，行为面大 |
| Selector 切片再拼回 `InteractiveSnapshot`（含丢掉 `isReverted`） | 后续另开 Task | 会碰 Web 网关视图，属行为/缺陷修复而非本清理 |
| Adapter `getGateway()` 绕过 Interactive Core（`/btw`、`/undo`） | 后续另开 Task | 交互语义迁移，不是删死代码 |
| 删除 `RemoteSandboxFactory` | 明确保留 | 企业沙箱扩展点，无生产实现不等于死协议 |
| 用内联 SVG 替换 `lucide-react` | 明确不做 | 依赖口味，不在「无主删除」 |
| 合并 TUI Tree-sitter 与 Web Shiki | 明确不做 | ADR 0003 双渲染器 |
| 大合并 Policy 22 文件 / 三套 Shell 白名单 | 后续另开 Task | 安全管线，误合会改审批 |
| 重编号两份 ADR 0003 | 明确不做 | 历史链接风险；本任务最多在 adr/README 加一句指向，不改文件名 |
| 改写 [Compose 工作模式.md](../architecture/Compose%20工作模式.md) 里仍把 Work Item 写成生产路径的段落 | HC-155 | 文档债跟引擎删除走 |

---

## 停点 1：删死代码、兼容别名、过期文档

用户怎么看：不需要新 UI。`bun run dev` 发一条普通 Build 消息，再在 Compose 模式走现有 Session 路径，表现与改前相同。开发者侧：`rg ComposeWorkflow packages/agent/harness_agent` 无命中；Controller 不再默认 noop gateway。

### 删除 HC-138 工作流栈

- [x] 负向测试锁定：`tests/test_architecture.py::test_hc138_workflow_stack_absent_from_production` 先红后绿。
- [x] 删除 `compose/workflow.py`、`state_machine.py`、`context_pack.py`、`methods/*.md`、`threads/compose_artifact_store.py`、`test_understand_plan.py`；`models.py` 去掉仅被该栈使用的 `ComposeRunState` / `ComposeRunStatus` / `ComposeStage` / `EvidenceItem`。`ThreadPersistence.compose_artifact_store()` 无调用方，只删该方法，未拆文件、未动迁移。
  - 证据：`pytest -q tests/compose/test_compose_session.py` 绿；生产包 `rg ComposeWorkflow` 无命中。
  - 未删 `work_item_engine.py` / `compose/activities/`。

### CLI 兼容别名与生产 noop

- [x] `gateway` 必填；删除 `options.agent` / `options.runtime` / `InteractiveScheduler` / `InteractiveResult` / `agent-port.ts`。Composition root 改为 `baseRuntime`。
  - 证据：`bun test tests/interactive` 211 pass；`bun run typecheck` 通过。
- [x] `controller.ts` 与 TUI adapter 生产路径不再调用 `createFallbackNoopGateway`。函数仍留在 `ports/agent-gateway.ts` 给测试用（复核：数十个 TUI 测试依赖它，搬到 tests/ 纯属搬家）。
- [x] **复核后保留** `semantic-tone.ts`：barrel 导出 + `semantic-tone.test.ts` 在锁 TUI/Web 语义色键，不是无引用死代码。

### Python 无主兼容符号

- [x] 删除 `run_execution.py` 测试包装；测试改走 `tests/support/run_stream.py`。
  - 证据：`pytest -q tests/host/test_run_coordinator.py tests/host/test_approval_protocol.py` 绿。
- [x] 删除 `ALWAYS_SAFE_COMMANDS`；危险命令断言改为按平台检查。
- [x] 删除 `_resolve_agent_engine_profile` 包装，唯一生产调用改为 `_resolve_agent_engine_spec(...).runtime_profile`。

### 过时工程附属物与文档

- [x] **复核后保留** `evaluations/zc133/`：仍是可运行的评测入口（`python -m evaluations.zc133`），研究文档还在引用，不是生产死代码。
- [x] **复核后保留** `test_hc166_plugin_management_red.py`：含 v2 registry 迁移等独有用例，`test_plugins.py` 未覆盖，未并入则不删。
- [x] 删除 `scripts/project/migrate-docs-layout.ts`。
- [x] 删除 `docs/项目分析.md`、`docs/Harness-Code-项目深度分析.md`、根 `tasks/plan.md`、`tasks/todo.md`。改写 `架构重构计划.md`：已归档 HC-092..097 不再列为当前待办。
  - `bun run docs:check` 被无关任务 HC-131 复核过期挡住，未跑通。
  - 未改 `Compose 工作模式.md`。

### 停点 1 验证

- [x] focused tests + typecheck：
  - `cd packages/agent && .venv/bin/python -m pytest -q tests/test_architecture.py tests/compose/test_compose_session.py tests/host/test_compose_rpc.py tests/host/test_run_coordinator.py tests/host/test_approval_protocol.py tests/policy/test_safe_commands.py tests/runtime/test_managed_agent_adapters.py` → 304 passed
  - stream 翻译：`test_server.py` 4 个相关用例 passed
  - `cd packages/cli && bun test tests/interactive tests/index.test.ts` → 211 pass
  - `bun test tests/tui/toast.test.ts tests/tui/status-modal.test.ts tests/tui/adapter-command-menu.test.ts tests/tui/app-interaction.test.ts tests/tui/btw-modal.test.ts` → 48 pass
  - `bun run typecheck` → 通过
- [x] `git diff --name-only` 不含 Work Item 引擎、file-icons、迁移仪式、AgentHost 拆分（仅内联一处 profile 包装）。
- [x] 更新 `tmp/handoff.md`。**停在这里等用户看。** 请用 `bun run dev` 发一条 Build 消息并走现有 Compose Session 路径确认无可见变化。

---

## 停点 2：合并 Skill 运行时与资源租约

用户怎么看：`bun run dev` 列出 Skill、启停一项、发一条会用工具的消息。没有新界面。关闭 Host 不报资源二次 close。

### Skill 单一模块

- [x] 架构测试 `test_skill_registry_has_one_definition` 先红后绿。
- [x] canonical `plugin_skills.SkillRegistry`；`SkillCatalogManager` 改为使用该 Registry；生产与测试 import 迁走；`skills.py` 不再定义第二份 `SkillRegistry`/`SkillError`。市场安装 journal 仍留在 `skills.py`（Host 管理 RPC 需要）。
  - 证据：`rg "class SkillRegistry" packages/agent/harness_agent` 仅 `plugin_skills.py`；`pytest -q tests/extensions tests/test_plugins.py tests/test_full_demo_plugin.py tests/test_architecture.py` → 161 passed, 2 skipped。

### 资源租约单一模块

- [x] `test_resource_scope_has_one_definition` 先红后绿。
- [x] 删除 `resource_ownership.py`；`ResourceScope` / `ResourceAccess` / `SharedResourceOwner` 并入 `resource_lifecycle.py`。Handle（池）与 Owner（Host MCP generation）关闭语义不同，保留两种 owner，但枚举与模块唯一。
  - 证据：`rg "class ResourceScope"` 一行；`pytest -q tests/test_resource_ownership.py tests/runtime/test_resource_lifecycle.py tests/runtime/test_agent_engine.py` 绿。

### 停点 2 验证

- [x] focused tests + `bun run typecheck`（通过）。
- [x] 本停点 diff 不含 Work Item / file-icons / 迁移拆文件。
- [x] 更新 `tmp/handoff.md`。**整份 Todo 已做完停点 1+2，停在这里等用户看。** 请 `bun run dev` 列出 Skill、启停一项、发一条会用工具的消息。
