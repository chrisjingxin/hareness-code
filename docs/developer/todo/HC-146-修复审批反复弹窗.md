# HC-146 修复审批反复弹窗执行 Todo

- 关联任务：[HC-146](../task/archive/HC-146-修复审批反复弹窗.md)
- 行为规格：[HC-146 Spec](../spec/HC-146-修复审批反复弹窗.md)
- 实施计划：[HC-146 Plan](../plan/HC-146-修复审批反复弹窗.md)

---

## 使用规则

- 严格按 WP1 → WP4 依赖顺序执行。
- 每一个工作包严格采用 **TDD**（测试先行 → 最小实现 → 重构）。
- **按停点交付**：一次只推进到一个可演示停点，更新对应 Todo 勾选并记录证据，交由用户检视后方可继续后续工作包。

---

## 开工前准备

- [x] 确认代码工作区状态，确保无冲突。
- [x] 确认 Task / Spec / Plan / Todo 齐备与状态流转完成。

---

## WP1：Shell 只读安全命令快放与 AutoEdit 删除放行

- [x] **先写失败测试**：
  - 断言 `evaluate_permission` 在 `approval_mode="auto-edit"` 与 `approval_mode="auto"` 下对 `ls`, `cat README.md`, `git status`, `git diff` 等只读安全命令返回 `"allow"`。
  - 断言 `evaluate_permission` 在 `approval_mode="plan"` 下对 `execute` 仍拒绝/不放行。
  - 断言 `_make_approval_preflight` 在 `auto-edit` 下对工作区内普通文件 `delete_file` 返回 `False`（不弹窗），对 `.git/config` 等敏感路径返回 `True`（弹窗）。
- [x] **实现策略逻辑**：
  - 修改 `packages/agent/harness_agent/policy/approval_policy.py` 中的 `evaluate_permission`：L3.1 放宽为 `approval_mode != "plan"`。
  - 修改 `packages/agent/harness_agent/runtime/agent.py` 中的 `_make_approval_preflight`：L3.1 放宽为 `approval_mode != "plan"`，并将 `auto-edit` 快速放行扩展到 `ToolKind.DELETE`。
- [x] **运行 focused tests 验证**：
  - 运行 `packages/agent/tests/policy/test_approval_pipeline.py` 和 `packages/agent/tests/policy/test_approval_policy.py` 确保全绿。

**完成信号**：
`auto-edit` 和 `auto` 模式下只读安全命令及非敏感删除测试全部转绿。

证据（2026-08-14）：`packages/agent/.venv/bin/python -m pytest packages/agent/tests/policy/` → 1034 passed, 2 skipped in 2.38s。`test_approval_pipeline.py`（16 用例）与 `test_approval_policy.py`（61 用例）全部通过。

**可演示停点 1（怎么看）**：
运行 `bun run dev`，切换模式为 `auto-edit` 或 `auto`。输入 `帮我看看当前 git status` 或 `查看 README.md 文件内容`，系统直接执行命令输出结果，不会再跳出 Execute 审批确认弹窗；在 `auto-edit` 模式下让 Agent 删除工作区内生成的临时测试文件，直接执行不弹窗。

---

## WP2：解释器裸根规则与已知子命令表

- [x] **先写失败测试**：
  - 断言 `extract_command_rule("python -m pytest tests/test_a.py")` 能产出规则（如 `python`），且 `matches_command_prefix` 能成功匹配 `python -m pytest tests/test_b.py`。
  - 断言 `extract_command_rule("ls packages")` 产出规则 `ls`（丢弃普通目录参数），且能匹配 `ls docs`。
  - 断言 `extract_command_rule("git commit -m msg")` 产出 `git commit`，且能匹配 `git commit --amend`。
  - 断言 `extract_command_rule("npm run build")` 产出 `npm run build`，`npm install lodash` 产出 `npm install`。
  - 断言 `extract_command_rule("rm foo.txt")` 产出空串 `""`，不生成裸根规则。
- [x] **实现规则提取与子命令表**：
  - 修改 `packages/agent/harness_agent/policy/bash_parser.py`：定义 `INTERPRETER_ROOTS` 与 `KNOWN_SUBCOMMANDS`，更新 `BARE_ROOT_FORBIDDEN`，重构 `extract_command_rule` 逻辑。
- [x] **运行 focused tests 验证**：
  - 运行 `packages/agent/tests/policy/test_bash_parser.py`（87 passed in 0.07s）以及全量 policy 测试（1035 passed, 2 skipped in 2.65s）。
  - 更新并验证 `packages/agent/tests/host/test_approval_protocol.py`（30 passed in 0.87s）。

**完成信号**：
所有解释器命令、已知子命令、无子命令工具及高危命令的规则生成与前缀匹配参数化测试全绿。

证据（2026-08-14）：`packages/agent/.venv/bin/python -m pytest packages/agent/tests/policy/test_bash_parser.py` 87 passed in 0.07s；`packages/agent/.venv/bin/python -m pytest packages/agent/tests/policy/` 1035 passed, 2 skipped；`packages/agent/.venv/bin/python -m pytest packages/agent/tests/host/test_approval_protocol.py` 30 passed in 0.87s。

**可演示停点 2（怎么看）**：
在 `default` 或 `auto-edit` 模式下，执行一次 `python -m pytest <文件A>`，在弹窗中选择「本项目允许」。再次让 Agent 执行 `python -m pytest <文件B>`，观察到命令直接自动放行执行，不再二次弹窗。

---

## WP3：Auto 模式分类器默认接通主模型

- [x] **先写失败测试**：
  - 构造 `approval_mode="auto"` 且 `approval_classifier=None` 的场景，断言 Host 解析出的分类器具有可用的 `SafetyClassifier`，且底层模型即为当前 Run 主模型。
  - 断言当分类器超时或返回异常时，预检与守卫安全回退到人工审批（Fail-Closed）。
- [x] **实现 Host 组装逻辑**：
  - 修改 `packages/agent/harness_agent/host/agent_host.py`：引入 `_resolve_approval_classifier`，当 `approval_mode == "auto"` 且 `spec.execution.approval_classifier` 未设置或找不到时，使用 `SafetyClassifier(model)` 兜底。
- [x] **运行 focused tests 验证**：
  - 运行 `packages/agent/tests/host/test_agent_host.py`（25 passed in 2.22s）与 `packages/agent/tests/policy/test_approval_policy.py` 中的分类器相关用例。

**完成信号**：
Auto 模式在未配 `[approval] classifier` 时能自动完成主模型分类，失败回退 ask。

证据（2026-08-14）：`packages/agent/.venv/bin/python -m pytest packages/agent/tests/host/test_agent_host.py -k "TestAutoModeClassifierAssembly" -v` → 4 passed in 0.92s；`packages/agent/.venv/bin/python -m pytest packages/agent/tests/host/test_agent_host.py -q` → 25 passed in 2.22s；`packages/agent/.venv/bin/python -m pytest packages/agent/tests/policy/` → 1035 passed in 2.21s。

**可演示停点 3（怎么看）**：
在未配置 `approval.classifier` 的前提下，切换到 `auto` 模式，发送一条包含未被白名单放行的普通命令，系统调用主模型自动判断安全性并直接放行。

---

## WP4：用户文档、架构文档与项目级检查

- [x] 更新 `docs/user/安全与沙箱.md`、`docs/user/交互使用.md` 中关于各模式放行范围、解释器规则与 Auto 默认分类器的说明。
- [x] 增量更新 `docs/developer/architecture/架构总览.md` 审批模式与规则持久化相关说明。
- [x] 运行 `bun run typecheck`、相关测试并记录测试证据。
- [x] 修复 `tool_risk.py` 中 `ToolKind.DELETE` 在 `auto-edit` 模式下的权限表缺失并补充回归测试。

**完成信号**：
所有文档与实现保持一致，测试全绿。

证据（2026-08-14）：
- `bun run typecheck` → Exited with code 0；
- `bun run project:check` → Exited with code 0；
- `packages/agent/.venv/bin/python -m pytest packages/agent/tests/policy/ packages/agent/tests/host/test_agent_host.py packages/agent/tests/host/test_approval_protocol.py packages/agent/tests/threads/test_multi_root_backend.py -q` → 1093 passed, 2 skipped in 3.99s。
