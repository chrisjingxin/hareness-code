# Todo: HC-159 下线后台任务假工具

> 对应 Plan：docs/developer/plan/HC-159-下线后台任务假工具.md。执行按停点交付，禁止一次做完所有 Todo。

## 停点 1：工具面与策略面删除

- [x] P1-1 新增负向测试（红）：test_harness_tools.py / test_deferred_tools.py / test_subagents_tools.py / tool-display-policy.test.ts
  - 完成信号：focused pytest 非零退出，失败项包含新增 isdisjoint / not in ALL_TOOL_NAMES / known 断言
- [x] P1-2 删除实现与接线（绿）：harness_tools.py 三注册块、tools_mode.py 整删、agent.py 契约三条、11 个策略/运行时文件、CLI 展示目录
  - 完成信号：focused pytest 与 bun test 全绿
- [x] P1-3 更新受影响测试并重命名 test_tools_mode_memory.py → test_tools_memory.py
  - 完成信号：该文件测试全绿，仓库无 tools_mode 引用

**用户怎么看**：`cd packages/agent && .venv/bin/python -m pytest -q <focused 列表>` 全绿；`grep -rn "task_output\|task_stop" packages/agent/harness_agent` 无命中。

## 停点 2：文档、全量检查、提交与归档

- [x] P2-1 更新 docs/user/subagent.md 与 docs/user/模型配置.md
  - 完成信号：subagent.md 工具表 17 项；两个文件 grep 不到三个工具名
- [x] P2-2 全量验证：agent 全量 pytest、cli bun test、project:check
  - 完成信号：三条命令全绿，证据写入 Task test_evidence
- [x] P2-3 按包提交（feat(agent) 与 feat(cli)+docs）
  - 完成信号：git log 两个聚焦 commit
- [ ] P2-4 task:complete 归档并 tasks:sync
  - 完成信号：Task 移入 task/archive/，看板无 HC-159
