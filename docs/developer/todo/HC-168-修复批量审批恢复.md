# HC-168 修复批量审批恢复执行清单

关联：[Task](../task/archive/HC-168-修复批量审批恢复.md) · [Spec](../spec/HC-168-修复批量审批恢复.md) · [Plan](../plan/HC-168-修复批量审批恢复.md)

只执行本文件的一个可演示停点；完成后等待父任务验收。不得归档 Task、提交或推送。

## 执行约束

- [x] 已读取仓库 `AGENTS.md`、README、架构总览并检查 `git status --short --branch`。
- [x] 已确认本任务只改 Agent 审批恢复边界，不改协议/UI，不修改第三方 LangChain。
- [x] 测试使用临时 HOME/workspace 和 fake 模型；不使用真实 API key、联网 MCP 或外部 Hook/process。
- [x] 保留初始 detached HEAD 与干净 worktree 边界，不执行 git add/commit/push/reset/checkout/clean。

## Red：真实恢复边界

- [x] 新增 `2×write_file + 1×task` 同一 interrupt 的 AgentHost 集成回归；完成信号：当前实现先出现决策数量不匹配或规则分类漂移的失败。
- [x] 新增恢复异常/取消不落地 staged rule 的回归；完成信号：实现前测试能观察到待提交状态边界。
- [x] 扩展 approve_once/reject 与 project 持久化失败的规则生命周期测试；完成信号：没有将非持久决定误写成权限规则。

## Green：一个垂直修复停点

- [x] `RunState` 保存 Run 私有 staged rule intent，并提供幂等 commit/discard；完成信号：收集阶段 session/project 真状态不变，取消/失败后 staged 为空。
- [x] `_collect_serial_approvals` 使用仅当前批次的 staged overlay；完成信号：同批后续同类动作仍可自动批准，但共享 `rules_provider` 不看见 overlay。
- [x] `ManagedAgentExecutor` 在 resumed `astream(Command(...))` 正常返回后触发 commit，在异常/取消前丢弃；完成信号：混合批次不再抛 ValueError，三个决定与三个原始 action 一一对应。
- [x] Build/child adapter 复用 Coordinator seam，project 写入继续传播异常；完成信号：无重复规则写入、无跨 Run 泄漏、project failure 仍 fail-closed。

## 停点验收：混合批次审批可恢复

- [x] 运行 HC-168 focused 集成测试并观察 `run.completed`；检查两个文件写入、task 最终结果和三项 action identity。
- [x] 运行相关 Agent approval/host/runtime 测试集合；记录精确 passed/failed/skipped 计数。
- [x] 运行 `bun run typecheck`、`bun run project:check`、`git diff --check`；单独记录无关基线或环境限制。
- [x] 更新 `docs/developer/architecture/架构总览.md`、Task 证据和本 Todo 的勾选状态。
- [x] 已由执行任务更新中文 `tmp/handoff.md`，父任务并已在主仓库独立复核设计、diff 与测试结果。

## 交付事项

- [x] 父任务验收通过；`task:complete` 因既有 HC-160 复核日期过期而在读取阶段阻塞，按其定义的等价元数据完成 HC-168 归档并同步移除看板条目。

## 本阶段证据

- Red：混合批次测试在实现前稳定复现 `ValueError: Number of human decisions (3) does not match number of hanging tool calls (1)`；实现后同一测试的 thread/project 参数均通过。
- Green：主仓库审批协议、混合批次、恢复失败/取消、ManagedAgentExecutor、Compose/Stage focused 集合 `134 passed in 19.50s`；全量 Agent 集合 `2616 passed, 6 skipped, 10 failed`。9 个 attachment 失败已在沙箱外复跑为 `9 passed`；剩余 Qwen 夹具失败已在 `origin/master` 基线复现。
- Checks：CLI 全量 `1028 passed, 1 skipped, 5 failed`，5 个 sidecar/loopback 用例沙箱外复跑 `5 passed`；`bun run typecheck`、project tests、protocol check、release check、`git diff --check` 通过；`bun run project:check` 仅被与本任务无关的 HC-160 复核日期过期阻塞。
