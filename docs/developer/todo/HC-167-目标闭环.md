# HC-167 目标闭环执行清单

关联：[Task](../task/HC-167-目标闭环.md) · [Spec](../spec/HC-167-目标闭环.md) · [Plan](../plan/HC-167-目标闭环.md)

每项必须先写最小失败测试，再写实现。一次只做到下一个可演示停点；完成本段后勾选、记录实际命令和结果、覆盖更新 `tmp/handoff.md`，然后停下等用户看过。未经用户要求不得继续下一停点。

## 实施前置门：DeepAgents 0.6.8

- [x] 新增 `packages/agent/tests/goals/test_rubric_compatibility.py`，用真实 graph + 结构化 mock model 验证 rubric mapping、独立 grader、`needs_revision → model`、custom stream、取消与 checkpoint cleanup。完成信号：已确认 0.6.8 的 iteration/event/terminal 差异，Harness adapter 边界可实现。
- [x] 固化 `ContextWindowMiddleware + RunContextSnapshotMiddleware + RubricMiddleware` 的组合顺序测试，并确认 proposal/grader 不获得 root tools。完成信号：`rtk packages/agent/.venv/bin/python -m pytest -q packages/agent/tests/goals/test_rubric_compatibility.py` → `4 passed`；无需升级或复制 SDK。

---

## 停点 1：创建、审核、恢复并自动续跑

用户怎么看：`rtk bun run dev`，Build 输入明确的 `/goal`，审核 criteria 后接受，Goal 卡片出现且自动开工；编辑、取消也可用。切到 `/web` 状态一致。重启打开同一 Thread 后 Goal 仍在但不会自动运行；发普通 Build 消息，Agent 能继续该目标。Compose 不显示 `/goal`。

### Protocol 与 Run 单路径

- [x] 修改 `packages/protocol/schema/v3.json`：minor 7→8，增加 Goal projection/request/mutation/interaction/event、`goal.read/manage` capability，并把 `run.start` 改成 tagged `input`。完成信号：schema 对旧 `message`、混合 input、未知字段和越界 payload fail closed。
- [x] 运行 generator，同步 `packages/protocol/src/generated.ts`、`validators.generated.ts`、fixture 与 Agent 端生成契约/摘要；更新 `packages/cli/tests/ipc/protocol-contract.test.ts` 和 `packages/agent/tests/protocol/test_protocol_contract.py`。完成信号：`rtk bun run protocol:generate`、`rtk bun run protocol:check` 全绿，Goal 写操作受 ControlLease gate。
- [x] 迁移 `packages/agent/harness_agent/host/run_coordinator.py` 的 `StartRun/fingerprint` 与 `agent_host.py` handler，按 `user|goal_proposal|goal_continuation` 分流且仍由同一 Coordinator 受理。完成信号：旧 message fixture 全部删除，三种 input 幂等身份与 busy/owner 行为通过 Host focused tests。
- [x] 迁移 `packages/cli/src/ipc/client.ts`、`interactive/ports/agent-gateway.ts` 和 `infrastructure/agent-client-gateway.ts` 的 typed RunInput。完成信号：普通 user Run 行为不变，内部 input 不要求伪 message，IPC/client tests 通过。

### SQLite Goal 事实

- [x] 新建 `packages/agent/harness_agent/goals/models.py` 与 `store.py`，先测试 projection、limits、criterion ID/digest、CAS、request/operation 幂等和稳定错误。完成信号：同 project/thread 只有一个 current 和一个 pending；不同 namespace 完全隔离。
- [x] 在 `thread_persistence.py` 把 schema v18 升到 v19，创建 Goal/current/pending/evaluation/activity/idempotency 表并暴露借用同一 connection/lock 的 `goal_store()`。完成信号：新库、v18 migration、close/reopen、过新/损坏库测试通过。
- [x] 实现首次 `create` 的空 Thread 索引事务。完成信号：Goal request 成功后 `threads.open` 可读，但 Transcript/标题没有伪用户消息；事务失败不留下半个 Thread 或 pending。

### Proposal 与 Host

- [x] 新建 `packages/agent/harness_agent/goals/proposal.py`，先只完成明确目标的结构化 `ready` 路径、limits 和只读工具预算；使用实际 primary model binding，不允许 mutation/任意 shell/update_goal。完成信号：mock model 的合法 proposal 可用，非法/超限/工具越权稳定失败。
- [x] 新建窄 `GoalService`，实现 `inspect`、`request(create)` 和 review 的 accepted/edited/cancelled apply transaction；`AgentHost` 只映射 Protocol、capability 和 ControlLease。完成信号：accept/edit 重新校验、迟到 response 拒绝、request retry 幂等、无 goal handle 取消且不激活。
- [x] 让 `goal_proposal` 走 RunCoordinator 与现有 `interaction.question/goal` resume seam，但不追加用户/助手 Transcript。完成信号：accepted response 后才 fanout，取消/断连唯一终态，proposal history 不进入模型历史。

### Interactive Core 与双端

- [x] 扩展 AgentGateway 的 typed goal methods；新增 `interactive/features/goal-feature.ts` 和 focused tests，集中处理 projection、pending、review、continuation。完成信号：Feature 不依赖 React/DOM/transport，TUI/Web adapter parity 通过。
- [x] 注册 `goal.manage` `/goal` 命令并实现无参数/show/status/其它 objective 的精确解析；Controller 为新 Thread 分配 ID，Compose 手输返回稳定 Build-only 提示。完成信号：菜单、补全、执行都来自 Registry；`/goal show JSON` 被当 objective，保留前缀非法参数不退化。
- [x] 扩展 shared `InteractiveState/Snapshot/selectors`，在 thread open/watch 时恢复 current/pending/latest evaluation/activity。完成信号：切 Thread、new、close、重连不会串 goal state，恢复本身不启动 Run。
- [x] TUI 实现 goal review（接受/编辑/取消）、Goal panel 和 viewer；补键盘、Esc、焦点、长文本与窄屏测试。完成信号：四周 UI 不包含 CAS/RPC 字符串，操作只派发 shared intent。
- [x] Web 实现同一 review DTO、Goal panel/viewer；补原生控件、ARIA、键盘和 adapter tests。完成信号：Web 不接触 AgentClient，状态和文案与 TUI selector 一致。

### Context 与自动 continuation

- [x] 新建 Goal Context adapter，把 active goal 渲染成转义、脱敏、有界的 `goal.state` Dynamic/Run ContextBlock，并在 Run preparation 生成冻结 `GoalRunBinding`。完成信号：revision、criteria digest 和 actual primary 在 Run 内不变；Prompt 明确 Goal 是用户数据而非权限。
- [x] 让 accepted proposal 返回持久 continuation token，由 GoalFeature 在 Run 空闲后 exactly once 启动 `goal_continuation`；Host 固定内部 instruction，不写 synthetic HumanMessage/Transcript。完成信号：response 重试、UI 重连、Controller 重建都不会重复启动。
- [x] 完成停点 focused checks：Protocol、GoalStore/Host、Interactive/TUI/Web、context/binding 和 `rtk bun run typecheck`。把实际命令/结果写入 Todo 或 Task。
- [ ] 按“用户怎么看”在正常终端手动走一遍并由用户验收。当前 Codex sandbox 内 `rtk bun run dev` 因用户级存储不可写而返回 `THREAD_STORE_UNAVAILABLE: CHECKPOINT_MIGRATION_LOCK_UNAVAILABLE`，不将环境限制伪装为功能失败。

### 停点 1 验证证据（2026-09-03）

- `rtk bun run protocol:check`：通过。
- `rtk bun run typecheck`：通过。
- `rtk bun run build`：通过，CLI 成功打包 149 modules。
- `rtk bun test packages/cli/tests/interactive packages/cli/tests/ipc packages/cli/tests/tui/presentation/bottom-area.test.ts packages/cli/tests/tui/presentation/views.test.ts packages/cli/tests/web/presentation/interaction-form.test.tsx packages/cli/tests/web/presentation/goal-view.test.tsx`：275 passed。
- `rtk .venv/bin/python -m compileall -q harness_agent && rtk .venv/bin/python -m pytest -q tests/goals tests/protocol/test_protocol_contract.py tests/test_architecture.py::test_non_host_modules_do_not_import_host tests/test_plugin_refresh.py tests/test_qwen_plugins.py`：142 passed，1 skipped。
- `rtk .venv/bin/python -m pytest -q tests/goals tests/protocol/test_protocol_contract.py tests/host/test_run_coordinator.py tests/host/test_agent_host.py tests/threads/test_thread_persistence.py`：229 passed，1 skipped；另 9 项仅因 sandbox 禁止绑定 `127.0.0.1` 失败。
- `rtk bun test packages/cli/tests/tui`：426 passed；唯一失败是被忽略的 `packages/cli/src/tui/.DS_Store` 触发既有目录白名单。
- `rtk bun test packages/cli/tests/web`：302 passed；6 项 loopback server 测试因 sandbox 禁止监听而失败。
- `rtk bun run project:check`：通过。
- `rtk bun run dev`：已尝试；sandbox 内因 `CHECKPOINT_MIGRATION_LOCK_UNAVAILABLE` 无法完成真实交互演示，等待用户在正常终端验收。
- 用户首次实测发现原始 `/goal` 输入未显示，且接受审核后一直等待。结构化日志显示 proposal 已到 `interaction.started(kind=goal)`，同时控制面 `mcp.status` 等待 30 秒超时，未出现 `interaction.completed`。根因是无 AgentEngine 的 proposal 在人工审核期间仍持有 snapshot reservation，阻塞 sidecar 串行读循环读取审核响应。
- 已增加两个回归门：原始 `/goal …` 立即进入本地用户时间线但不写 Host Transcript；proposal 在模型调用与人工审核前释放幂等 snapshot reservation。`rtk bun test packages/cli/tests/interactive/goal-feature.test.ts`：5 passed；`rtk .venv/bin/python -m pytest -q tests/goals/test_goal_proposal.py`：4 passed。
- 修复后扩大验证：CLI interactive + Goal TUI/Web focused 260 passed；CLI IPC + interactive 199 passed；Agent goals/run coordinator/approval 91 passed；`rtk bun run typecheck` 与 `rtk bun run build` 通过。包含 attachment 的 Agent 扩大集为 121 passed、1 skipped，另 9 项仅因 sandbox 禁止绑定 `127.0.0.1` 失败。
- 用户第二次实测发现审核完成且 Goal 已 active，但未自动执行。日志确认 `interaction.completed → run.completed → goal.inspect` 均成功，却没有第二次 `run.start`。回放真实终态发现 Host 输出 `context.goal_continuation`，CLI 测试却伪造成顶层 `context.action`，导致 token 未被识别；已收敛为嵌套 canonical 字段并用真实形状覆盖自动续跑回归。
- 第二次修复验证：`rtk bun test packages/cli/tests/interactive/goal-feature.test.ts` 先稳定复现“期望 2 个 Run、实际 1 个”，修复后 5 passed；CLI IPC + Interactive 199 passed；`rtk bun run typecheck`、`rtk bun run build`、`rtk bun run protocol:check`、`rtk bun run project:check` 与 `rtk git diff --check` 均通过。
- 用户第三次实测在 proposal 审核前收到 `GOAL_OBJECTIVE_INVALID`。日志确认模型调用约 9 秒后在 `run_execution` 失败，且没有 `interaction.started`；原实现依赖提示词要求模型输出裸 JSON，再直接解析供应商自由文本，未复用 dcode 的 forced structured output。现已改为唯一的 `GoalDraftOutput` tool schema 路径，保留 unknown field、文本预算和 criteria 非空校验，不接受自由文本 fallback。
- 第三次修复验证：先用 provider content block 构造出原解析失败，再以 dcode 的 canonical 方案替换；`rtk .venv/bin/python -m pytest -q tests/goals/test_goal_proposal.py` 6 passed，其中 `httpx.MockTransport` 直接验证 OpenAI-compatible wire 携带强制 `GoalDraftOutput` tool choice 并正确还原草案；Goal/RunCoordinator/Approval focused 集 93 passed；含 AgentHost 的扩大集 108 passed、1 skipped，另 9 项仍仅因 sandbox 禁止绑定 `127.0.0.1` 失败。`compileall`、`project:check` 与 `git diff --check` 通过。日志新增无正文的 `goal.proposal.model.completed|invalid` 诊断事件。

---

## 停点 2：Proposal 与完整生命周期

用户怎么看：创建一个有大歧义的目标，回答问题；review 中带 feedback 驳回后重生，再接受。演示 amend、replace、pause、resume、clear。Agent 运行中操作应显示 queued，当前 Run 继续旧 revision，终态后只应用一次；重启后 pending/review 能恢复。

### Proposal 完整行为

- [x] 扩展 Criteria Agent readiness：小歧义输出 assumptions，大歧义每轮最多 3 问题，最多 2 轮；最近对话与仓库工具预算按 Spec。完成信号：缺失交付物/不可观察/冲突约束测试正确分类，超过上限为 `GOAL_OBJECTIVE_UNCLEAR`。
- [x] 实现 rejected+feedback 的同 Run 重生，以及 amend/replace base identity。完成信号：旧 goal 在 review 期间仍 active；accept 后 amend 同 identity 加 revision，replace 新 identity 并退休旧 goal；过期 base 全部 CAS 拒绝。

### Lifecycle 与队列

- [x] 在 GoalService/Store 实现 pause/resume/clear/cancel_pending 和状态转换不变量。完成信号：幂等 notice 不加 revision，complete/无 goal 非法转换返回 Spec 错误；clear 保留历史。
- [x] 实现单 pending write 与 active Run 队列；Run terminal 顺序固定为 evaluation→拒绝失效 completion→apply operation→返回 ready/continuation。完成信号：并发第二写拒绝，旧 Run binding 不变，每个 operation/request 只应用一次。
- [x] 实现 Host restart reconcile：reviewing 恢复同一草案、queued→ready、drafting/clarifying→ready、pending mutation 在无 active Run 时应用。完成信号：kill/reopen fixtures 不信任半截输出，也不会因 open 自动续跑。
- [x] 扩展 `threads.open/watch` 的 Goal projection 和最近 50 条独立 activity；失败不以空状态掩盖存储错误。完成信号：activity 不进入 Transcript/标题/context，顺序稳定且有界。

### 命令与展示

- [x] GoalFeature/Registry 完成 amend/pause/resume/clear 参数矩阵和 pending notice；resume 只在有效状态返回 continuation。完成信号：命令 tests 覆盖空参、保留前缀、非法参数和 Compose hidden。
- [x] TUI/Web review 补 rejected+feedback，panel/viewer 显示 pending、revision、note 和 activity。完成信号：两端同一 semantic decision；恢复 review 不重新调用 Criteria Agent。
- [ ] 完成停点 focused checks与手动演示，更新 `tmp/handoff.md` 后停止。

### 停点 2 验证证据（2026-09-03）

- `rtk .venv/bin/python -m pytest -q tests/goals`：37 passed；覆盖 readiness/澄清轮数与问题数、只读仓库工具预算、驳回重生、amend/replace、生命周期、队列、重启对账和最近 50 条 activity。
- `rtk .venv/bin/python -m pytest -q tests/host/test_run_coordinator.py`：45 passed；`rtk .venv/bin/python -m pytest -q tests/protocol/test_protocol_contract.py`：15 passed。
- CLI Goal/Interaction/TUI/Web focused 集：93 passed；CLI 启动与 Plan 续跑回归：38 passed。
- `rtk bun run typecheck`、`rtk bun run build`、`rtk bun run protocol:check`、`rtk bun run project:check` 与 `rtk git diff --check`：全部通过。
- Agent 全量：2607 passed、7 skipped；唯一 9 项失败均因 sandbox 禁止绑定 `127.0.0.1`。CLI 全量：1073 passed、1 skipped；剩余失败均为 sandbox loopback/诊断日志权限与用户工作区既有 `.DS_Store` 白名单问题，本次发现的 Goal capability 与 Plan 续跑回归已修复并通过定向测试。
- `rtk bun run dev`：已再次尝试；当前 Codex sandbox 仍返回 `THREAD_STORE_UNAVAILABLE: CHECKPOINT_MIGRATION_LOCK_UNAVAILABLE`，因此“用户怎么看”中的真实交互演示保留为待用户在正常终端验收，最后一项暂不勾选。
- 用户严重验收发现 Criteria model 输出非法结构时，按 Esc 虽收到 `run.cancelled`，客户端却把 Host 恢复为 `ready` 的同一 proposal 立即重启，表现为“已取消但仍在运行”并重复展示结构化校验错误。根因是显式取消与进程意外中断共用 proposal 恢复路径，且 CLI 不区分 proposal 的 completed/cancelled/failed 终态。现已修复为：Host 对 `cancel_requested` 的 `goal_proposal` 原子清除 pending 并记录独立取消 activity；CLI 对 cancelled/failed proposal 只刷新权威状态，不自动重启。正常 Run 终态消费 queued proposal、Host 崩溃后恢复 ready 的语义保持不变。
- Esc 修复先以两条红灯稳定复现：CLI 观察到取消后 Run 数由 1 变 2；Agent 观察到 pending 从 `drafting` 被错误恢复成 `ready`。修复后 `rtk bun test packages/cli/tests/interactive/goal-feature.test.ts` → 10 passed，`rtk .venv/bin/python -m pytest -q tests/goals/test_goal_host_lifecycle.py` → 5 passed；扩大验证为 CLI Interactive 172 passed、Agent Goal/RunCoordinator 83 passed、TUI Esc/真实交互 33 passed，`rtk bun run typecheck`、`rtk bun run build` 与 `rtk git diff --check` 通过。
- 用户再次验收时收到 `GOAL_REPOSITORY_RESULT_LIMIT`。对应 Agent 日志确认 proposal Run 以 `GoalStoreError` 失败；同一仓库 `glob("**/*.md")` 可稳定复现 51,827 字符结果。修复先以红灯证明宽查询会杀死 Run，随后改为返回不含部分正文的有界“缩小查询”结果，保留调用次数/累计上下文/显式越界 fail closed；工作区扫描跳过外部 symlink，但显式读取仍拒绝。另将既有 `goal.proposal.model.completed|invalid` 纳入 Diagnostic Log v1，记录 tool kind、原始结果大小、受限次数和稳定错误码，不记录查询参数、路径正文或模型正文。修复后真实仓库 `**/*.md`（51,827）与 `**/*`（3,625,557）均返回 84 字符提示且 `violation=None`；Agent Goal+Diagnostic 81 passed、CLI Diagnostic 13 passed、Host/Protocol 30 passed/1 skipped，另外 9 项仅因 sandbox 禁止 loopback 失败；`protocol:check`、`typecheck`、149 modules build、`project:check`、`compileall` 与 `git diff --check` 通过。

---

## 停点 3：独立验收闭环

用户怎么看：设一个包含改代码和测试的目标。第一次遗漏测试时，Timeline 显示 checking/gap，Agent 在同一个 Run 补做；全部通过后 satisfied 且 Goal complete。用 mock/fixture 验证 max=1、取消和 grader error 都不会完成。

### Rubric 与权限

- [ ] 扩展 `RunExecutionBinding/AgentEngineProfile`，保存 goal_id/revision/criteria_digest、actual grader fingerprint、max iterations、goal_backed 和窄工具身份。完成信号：绑定不含秘密，配置差异不会错误复用旧 AgentEngine。
- [ ] 新建 `goals/rubric_adapter.py`，按 stable criterion ID 渲染 canonical rubric并实例化原生 0.6.8 `RubricMiddleware`。完成信号：无 rubric no-op；active Build 才 grading；iteration 映射为 1-based；last needs_revision 归一化为 max。
- [ ] 在 root Build Agent 注册 `update_goal(complete|blocked,note)`，从 child/proposal/grader schema 移除。完成信号：complete 只保存当前 run/revision 的申请；blocked 立即 CAS 并停止 grading；Plan 下 complete 拒绝。
- [ ] 建立 current root Run verification evidence registry 和 `rerun_verification(evidence_id)`。完成信号：只允许 exact command/cwd/sandbox/脱敏 env；副作用、安装、发布、网络写入和任意字符串注入拒绝；重跑继续按当前审批。
- [ ] 构造 grader 的最小只读工具视图。完成信号：`effective_goal_tools ⊆ effective_primary_tools` 在 default/auto-edit/auto/yolo 均成立，grader 无 file mutation/task/memory save/write MCP/external message。

### Evaluation 与完成事实

- [ ] 翻译 `rubric_evaluation_start/end` 为 `goal.evaluation`，校验 goal/run/grading identity、1-based iteration 和 payload limits；先写 GoalStore 再 fanout。完成信号：SDK private event 不越过 Agent，乱序/重复/迟到事件幂等或拒绝。
- [ ] 实现 exact criterion ID coverage。完成信号：缺项、重复、未知 ID、satisfied+fail 矛盾、截断内容均不能成为 satisfied；SDK exception 脱敏为 `grader_error`。
- [ ] 实现唯一 Completion Guard transaction。完成信号：Spec 11.4 的每个 AND 条件都有单独反例；只有当前正常 Run 的当前完整 satisfied 能 active→complete。
- [ ] RunCoordinator 终态接入 Goal reconcile。完成信号：取消/failed/旧 revision/pending invalidation/重复 terminal 不完成；配置类 queued mutation不阻塞当前 frozen satisfied。

### Timeline

- [ ] Shared Timeline 增加 goal activity item和恢复投影；不伪装 assistant/tool。完成信号：checking/result、criteria/gap/grader profile 顺序稳定，child timeline 不混入 root goal activity。
- [ ] TUI/Web 原生渲染 grader activity，支持展开与长文本降级。完成信号：两端 focused tests、ARIA/键盘测试通过，blocked/complete notice 不进入 canonical Transcript。
- [ ] 完成停点 focused checks与手动演示，更新 `tmp/handoff.md` 后停止。

---

## 停点 4：配置、模式和历史操作收敛

用户怎么看：切换 grader 模型和 max iterations；Plan 只显示目标不验收，`/btw` 不参与；blocked 后普通回复恢复。完成后 undo，Goal 回 active；redo 不恢复 complete。重启并在 TUI/Web 切换，状态一致。

### Grader 配置

- [ ] GoalStore/Service 实现 grader `inherit|profile`、actual profile audit 和 max iterations 默认 3、范围 1～20、clear。完成信号：布尔、0、负数、>20、未知/不唯一/策略禁止 profile 稳定拒绝；重启恢复。
- [ ] GoalRunBinding 通过现有 model resolver 解析 grader profile；不可用时在 Run 受理前 `GOAL_GRADER_MODEL_UNAVAILABLE`，不回退 primary。完成信号：inherit 等于该 Run actual primary，独立 profile 不泄漏 endpoint/key/provider 参数。
- [ ] `/goal model` picker与 `/goal max-iterations` 命令/双端 viewer 完成。完成信号：运行中 mutation queued 且只影响下一 grading run；实际 grader identity 可查看、可审计。

### 模式、阻塞与历史

- [ ] 固化 goal-backed matrix：普通 active Build=true；Plan/Compose/direct shell/side question/proposal=false；paused/blocked/complete=false。完成信号：每格 Host/runtime test 通过，Plan 能看 goal context 但不能 complete。
- [ ] 实现 blocked 后 resume 或普通 Build 消息原子恢复 active，并只向本 Run 投影一轮 `prior_blocker`。完成信号：受理事务失败则用户消息不开始；Run 终态清 prior blocker。
- [ ] 将 Goal evaluation stale/complete→active 纳入 `threads.undo` transaction，redo 不恢复 satisfied。完成信号：conversation/code/both、回滚失败、redo、迟到 event 测试通过。
- [ ] 补 default/auto-edit/auto/yolo 权限矩阵回归。完成信号：Goal continuation 不选择更宽模式，criteria review 在所有模式都必须人工确认，Goal 不生成 allow rule。

### 文档、整体验证与 review

- [ ] 更新 `docs/user/交互使用.md`：命令、proposal、状态、续跑、Timeline、非后台语义。
- [ ] 更新 `docs/user/模型配置.md` 与 `docs/user/安全与沙箱.md`：grader 选择、实际模型、权限不扩大、受控复验和审批可能性。
- [ ] 新增 `docs/developer/architecture/目标闭环.md`，增量更新 `架构总览.md` 与 `斜杠命令体系.md`；文档只描述最终 canonical 路径。
- [ ] 跑完整检查：`rtk bun run protocol:check`、Agent goals/host/runtime/threads tests、CLI interactive/tui/web/ipc tests、`rtk bun run typecheck`、`rtk bun run build`、`rtk bun run test`、`rtk bun run project:check`。完成信号：全部通过；受 sandbox 限制的宿主测试只按规范记录跳过。
- [ ] 使用 `agent-skills:code-review-and-quality` 对照 Task/Spec/Todo 做五轴 review，修复本任务问题并把结论、测试、用户验收和版本影响写回 Task。完成信号：无未决 finding、Todo 全勾选；未经用户明确要求不自动 commit/PR/发布。
- [ ] 用户验收后运行项目 task 完成流程，将 Task 移入 archive、同步看板并更新 `tmp/handoff.md`。完成信号：Task/Spec/Plan/Todo/架构/用户文档链接和状态检查全绿。
