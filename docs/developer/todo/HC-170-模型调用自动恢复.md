# HC-170 模型调用自动恢复执行清单

关联：[Task](../task/HC-170-模型调用自动恢复.md) · [Spec](../spec/HC-170-模型调用自动恢复.md) · [Plan](../plan/HC-170-模型调用自动恢复.md)

执行代理不得改变重试预算来源、错误分类、ID invariant 或停点范围。阶段 4 已完成；当前更新最终验证证据并等待用户验收，不提交、不推送、不归档。

## 阶段 1：RED（已完成）

- [x] 新增 `tests/runtime/test_model_output_guard.py`：name 缺失/空白、结构不可用、`invalid_tool_calls` 与 id 缺失行为先失败；合法 unknown tool 不应被 guard 拒绝。
- [x] 为 executor 增加 MALFORMED_TOOL_CALL、5xx、预算、确定性错误与 timing/diagnostic 测试；429/transport 继续由 canonical recovery focused tests 覆盖。
- [x] 为 Build/Inline 纵向 fake-agent 增加首轮畸形、次轮合法用例，并固定 delegation 稳定码回传。

## 阶段 2：GREEN（已完成）

- [x] 新建 `packages/agent/harness_agent/runtime/model_output_guard.py`：sync/async 校验、稳定 ID 生成、`MalformedToolCallError`。
- [x] 在 root/Inline child middleware 装配 guard；未知/未授权合法调用继续走现有 policy ToolMessage。
- [x] 建立 runtime canonical `BoundedProviderRetry`，迁移 Compose/Stage 并覆盖 429/Retry-After、5xx、transport、MALFORMED_TOOL_CALL 与非 retryable 错误。
- [x] executor 在 retry 前回滚尚未公开 output 的 session/observer capture；content/reasoning 保持实时，tool signal 等模型节点确认；已完成 Tool 批次保持既有即时提交；retry wait 和 model diagnostic 仍只写稳定脱敏字段。
- [x] RunPreparation/resolved spec 传递 `ModelSettings.max_retries`；gateway 关闭 SDK 重试，保留单一 executor owner。

## 阶段 3：可演示停点（已完成）

用户怎么看：离线运行 Build 根和 Inline child fake-model 用例；两者都应看到 attempt 1 畸形被丢弃、attempt 2 成功，且不存在 capability-policy ToolMessage、由 literal `<tool_call>` 误判产生的失败、孤儿 Transcript 或 checkpoint；长回答的首片正文应在模型未结束时可见。

- [x] Build 根 Run 纵向用例通过。
- [x] Inline child 纵向用例通过；`MALFORMED_TOOL_CALL` 稳定码不会被 delegation 改写为普通未知异常。
- [x] `MALFORMED_TOOL_CALL`、retry wait、attempt 和脱敏 `model.failed`/`model.retry_scheduled` 可断言。
- [x] 更新 Task 的阶段证据并写 `tmp/handoff.md`，停止等待用户验收。

## 阶段 4：边界与收尾（已完成，当前待验收）

- [x] 合法缺少 ID 的 assistant/tool/下一轮请求全链路同 ID。
- [x] unknown/unauthorized tool 原 ID structured error 不触发 provider retry。
- [x] 400、401/403、取消、审批拒绝、能力策略拒绝不重试；预算耗尽只发一个稳定 `run.failed` 且临时 provider 错误 `retryable=true`。
- [x] Compose/Managed Plugin/Team 与现有取消、终态和 checkpoint 语义回归通过。
- [x] 更新 `docs/user/故障排查.md` 与 `docs/developer/architecture/架构总览.md`。
- [x] 直接摘要、`/btw`、AUTO 分类器和 evaluation runner 经过同一 bounded provider retry helper；SDK `max_retries=0`。
- [x] 失败模型 attempt 中尚未公开的 tool signal、Build Transcript 与 graph checkpoint capture 可回滚；已实时公开的 content/reasoning 保留一次，已完成 Tool 边界保持即时提交。
- [x] 已公开 content/reasoning 后再发生临时 provider 错误时不自动 retry、不重复显示，并收敛为 `PROVIDER_OUTPUT_INTERRUPTED`；终态 `retryable=true` 只供用户发起新 Run。
- [x] retry delay 接收 provider attempt；无 Retry-After 时按 `base_delay * attempt` 线性增长并受上限约束。
- [x] 运行并记录 focused、packages/agent、`bun run typecheck`、`bun run project:check`、`bun run test`、`git diff --check`：HC-170 focused `460 passed, 2 skipped, 1 warning`；Agent 全量 `2671 passed, 5 skipped, 1 failed, 2 warnings`，唯一失败为未改动 Qwen bare-node 宿主子进程用例；typecheck/diff check 通过；root test 为 `1031 pass, 1 skip, 5 fail`，失败是 loopback/sidecar 宿主能力；project check 被无关 HC-160 过期复核阻塞。
