# HC-170 执行中可用运行时命令执行清单

关联：[Task](../task/HC-170-执行中可用运行时命令.md) · [Spec](../spec/HC-170-执行中可用运行时命令.md) · [Plan](../plan/HC-170-执行中可用运行时命令.md)

每项必须先写最小失败测试，再写实现。一次只做到下一个可演示停点；完成本段后勾选、记录实际命令和结果、覆盖更新 `tmp/handoff.md`，然后停下等用户看过。未经用户要求不得继续下一停点。

---

## 停点 1：执行中能打字、本地命令能跑、Esc 不误杀

用户怎么看：`bun run dev` 发一条会跑一会儿的 prompt。执行中输入栏可打字；`/status` 打开浮层，Esc 关掉后任务仍在跑；输入普通文字被拒绝且草稿还在；无浮层 Esc 不停任务；`Ctrl+C` 才停且草稿还在。Web：能提交 `/status`，Esc 不停，点停止才停。

### Registry 与提交路由

- [x] 删除 `allowedDuringRun`。为 builtin / plugin 命令按 Spec 5.1/5.2 写入 `safety.runtime`。未标 `allowed` 的执行中 disabled。完成信号：`tests/interactive/commands.test.ts` 覆盖 `/status` available、`/compact` `/skills` `/new-work` 与 Plugin disabled，原因「当前任务结束后可用」。
- [x] `availability.state === "disabled"` 时 Dispatcher 返回 `unavailable`，Controller 映射为 `rejected`（不清草稿）。未知斜杠仍 `accepted` + notice。完成信号：`runtime-submit.test.ts` running 时 `/compact` `/resume` rejected；`/not-a-cmd` 本地未知提示。
- [x] running 时普通文字与 `//` 转义 `rejected` `busy`。完成信号：`runtime-submit.test.ts` 不第二次 `run.start`。
- [x] `present` 仅放行 `status`、`agents`。完成信号：running 提交 `/status` `/agents` accepted 且带对应 present effect。

### 焦点、键盘、Web submit

- [x] TUI `InputBar`：`activeRun` 不再失焦；压缩中与 picker 仍失焦。placeholder「正在执行；Ctrl+C 中断」。完成信号：`input-bar.test.ts`。
- [x] TUI `resolveShortcut`：无浮层 Esc = `hint-interrupt`；执行中 `Ctrl+C` 无论草稿均为 `cancel-run`。完成信号：`shortcuts.test.ts`。
- [x] Adapter：`cancel-run` 保留草稿；`hint-interrupt` toast「中断请用 Ctrl+C」。完成信号：`adapter-runtime-input.test.ts`。
- [x] Web `submit()` 不再因 `activeRun` 丢弃；Composer / `web-app` Esc 派发 `interrupt-hint`；placeholder「正在执行；点停止中断」。停止按钮仍 `cancel-run`。完成信号：composer / web adapter / web-app 测试。

### 停点 1 验证

- [x] focused tests：commands、runtime-submit、command-feature、shortcuts、input-bar、adapter-runtime-input、web composer/adapter/web-app、views、web timeline。168+78+89 相关文件通过。
- [x] `bun run typecheck` 通过。
- [ ] 按「用户怎么看」在终端和 Web 各走一遍。**停在这里等用户看。**

---

## 停点 2：`/btw` 与 `/goal`

用户怎么看：执行中 `/btw 这个报错是什么意思`，TUI 与 Web 都出旁路面板，主任务继续，主时间线没有这轮问答。空参 `/goal` 打开查看浮层，Esc 关掉后还在跑。`/goal pause` 提示排队或已应用，当前 Run 不中止。审批弹出时查看浮层消失，底部变成审批。

### Web BTW

- [x] Web Adapter 处理 `side-question`：打开最小面板（loading / ready / error、问题、回答、关闭、复制）。Esc 关面板不取消主 Run。空参 `/btw` 仍是用法 notice。完成信号：web adapter/presentation 测试；主 timeline 不加 BTW 消息。CLI `WebUiGateway` 在回传 outcome 前补全 `sideQuestion`。

### 执行中查看浮层

- [x] 执行中空参 `/goal`：TUI/Web 打开独立 overlay，不进入 GoalDock。空闲路径不改。完成信号：running 时 snapshot 无 `interaction.type === "goal"`，有 overlay 状态。
- [x] 执行中 `/plan-view` 且无挂起计划审批：同样 overlay。有挂起计划审批则仍 `focus-plan`。完成信号：对应 adapter/controller 测试。
- [x] `pendingInteraction` 变为审批 / 目录信任 / 问答 / 计划审批 / Goal 审核时，关闭 Goal/Plan/Status/BTW overlay。完成信号：先打开 overlay 再注入 approval，overlay 关闭且底部为审批。

### Goal 写操作

- [x] running 时 `/goal pause|resume|clear|<文本>` 走既有 GoalFeature；queued 时 notice，不 `startRun`、不 cancel。完成信号：goal-feature 或 controller 测试在 activeRun 下 mutate/request 不启动第二 Run。

### 停点 2 验证

- [x] 本段 focused tests + `bun run typecheck`。见 `tmp/handoff.md`。
- [ ] 按「用户怎么看」演示 TUI 与 Web，写证据，更新 `tmp/handoff.md`。**停在这里。**

---

## 停点 3：其余命令、退出确认、文档

用户怎么看：执行中 `/agents` 能打开只读列表；`/teams generate …` 被拒绝且草稿还在；`/quit` 先确认，选否继续跑。文档写明 Esc 与取消键。

### 其余放行 / 子命令

- [x] `/plan` running 行为保持 Spec：空参/`exit` 切档；带目标只切档不 `submit-prompt`。补回归测试。
- [x] `/mcp` running 只读刷新，不走 add/remove。`/agents` running 可 `present`。
- [x] `/teams generate|run` running 时 `rejected` 留草稿；`list/show/status/cancel` 仍执行。
- [x] `/quit` `/q`：`confirmation: "when-running"`。running 时确认「当前任务将被中止」；取消则继续；确认才 `request-exit`。压缩中无 Run，不走该确认。完成信号：dispatcher/controller 测试。

### 文档

- [x] 更新 `docs/user/交互使用.md`：执行中可输入、放行/禁用摘要、Esc 只关浮层、TUI `Ctrl+C` / Web 停止按钮取消、placeholder 文案。
- [x] 更新 `docs/developer/architecture/斜杠命令体系.md`：执行中按能力表放行，不再暗示整栏禁用输入；写上 `runtime` policy 与失败关闭。
- [x] 如 TUI 表现层文档仍写执行中只能 Esc，同步改 `TUI表现层.md` 输入栏说明。

### 停点 3 验证

- [x] 本段 focused tests + `bun run typecheck`。见 `tmp/handoff.md`。
- [x] `bun run project:check`（若被无关过期 Task 挡住，记入 handoff，不改无关 Task）。
- [ ] 按「用户怎么看」演示，写证据，更新 `tmp/handoff.md`。**整份 Todo 完成前再跑包级测试并准备 review。**
