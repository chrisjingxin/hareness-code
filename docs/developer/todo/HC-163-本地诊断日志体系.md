# HC-163 本地诊断日志体系执行清单

关联：[Task](../task/HC-163-本地诊断日志体系.md) · [Spec](../spec/HC-163-本地诊断日志体系.md) · [Plan](../plan/HC-163-本地诊断日志体系.md)

每项行为变更先写最小失败测试，再写最小实现。一个停点做完后：勾选本节、记录命令与结果、更新 `tmp/handoff.md`，然后停下等用户查看。

停点 1–3 已完成。原停点 4 按 Spec 增量拆成 4a（Thread 中文主视图 + 已有事件名字）、4b（Skill/Hook/execution 章节）、4c（cursor/上限）。一次只做下一个停点。

---

## 停点 1：两个进程都能安全异步落日志

用户怎么看：

```bash
HARNESS_LOG_LEVEL=debug bun run dev -- --non-interactive --message "只回复 ok"
```

查看 `.harness/logs/<date>/`：有 CLI/Agent 独立 JSONL，含 logging/process/initialize 事件；stdout 只有 `ok`。

### 1.1 Diagnostic Log v1 contract

- [x] 先在 Protocol tests 加失败用例：最小有效 envelope、每事件 fields、`additionalProperties=false`、level/event 不匹配、8 KiB 上限、NaN/Infinity、敏感 key。完成信号：未实现 v1 schema 时测试稳定失败。
- [x] 新增 `packages/protocol/diagnostic-log/schema/v1.json` 与事件/query schema；envelope、事件目录、级别和字段与 Spec 一致。完成信号：所有正反 fixture 符合预期。
- [x] 扩展 generator，生成 TypeScript 类型/validator/digest 和 Python generated contract/validator/digest；不手写双份事件目录。完成信号：`bun run protocol:generate && bun run protocol:check`。
- [x] 增加 `@za38/protocol/diagnostic-log` export 和 TS↔Python 互验 fixture。完成信号：任一端产生的 v1 记录被另一端接受，多字段/错类型两端一致拒绝。

### 1.2 TypeScript runtime

- [x] 先在 `packages/cli/tests/diagnostic-log/runtime/` 写 queue 失败测试：4096 条/8 MiB 双上限、128 条/256 KiB WARN/ERROR 保留、DEBUG → INFO 淘汰、record sequence 不复用。
- [x] 实现 TS `DiagnosticLog.child/debug/info/warn/error` 与 immutable context；fields 不能覆盖 envelope ID。完成信号：调用点不需要组 JSON/envelope。
- [x] 实现内存校验/脱敏/限长/编码与有界入队；contract violation/oversize 不抛回业务路径。完成信号：反例只增计数并合并为安全事件。
- [x] 先写 slow/failing writer、partial write、close timeout 失败测试，再实现单后台 writer 和最多 2 秒 close。完成信号：`log.*` 不 await 磁盘，writer 失败后 disabled 且不影响主流程。
- [x] 先写 path/rotation/retention 失败测试，再实现平台 home API、exclusive-create、close-before-rename、closed-only retention、active/orphan 不动。完成信号：`cd packages/cli && bun test tests/diagnostic-log/runtime`。
- [x] 删除 TS 生产日志路径中的同步文件 I/O。完成信号：搜索无任何日志热路径 `writeSync`。

### 1.3 Python runtime

- [x] 先在 `packages/agent/tests/diagnostic_log/` 复制同一 queue/file lifecycle 行为表的失败测试，使用 fake clock/home/writer。
- [x] 实现 Python `DiagnosticLog.child/debug/info/warn/error` 和对等 validator/context 语义。完成信号：TS/Python 对同一 fixture 给出同一接受/拒绝结果。
- [x] 实现单后台 worker、有界 queue、batch append、rotation/retention、disabled 降级和 2 秒 close。完成信号：asyncio Run 在 slow writer 下继续，丢弃策略与 TS 一致。
- [x] 验证 POSIX 目录 `0700`/文件 `0600`，Windows adapter 不把 mode bit 当 ACL 保证。完成信号：`cd packages/agent && .venv/bin/python -m pytest -q tests/diagnostic_log`。

### 1.4 配置、Protocol 和启动接入

- [x] 先为 `[diagnostics]` 写配置失败测试：level 枚举、retention/capacity 范围、`max_file_mib <= max_total_mib`、`HARNESS_LOG_LEVEL` 优先、`applies_to=restart`。
- [x] ConfigManifest/Config/ConfigChangeService 实现 `[diagnostics]`，不激活 `[telemetry]`。完成信号：`tests/config` focused tests 通过。
- [x] 先修改 JSON-RPC contract tests，再给 `InitializeResult` 加 required effective diagnostics；生成 TS/Python 两端。完成信号：`bun run protocol:generate && bun run protocol:check`。
- [x] 修正所有命令的 `connection.project.id`：不打开 Thread SQLite 也返回真实 workspace fingerprint，不再返回 `echo`。完成信号：管理命令测试断言 SQLite 未打开但 project id 正确。
- [x] 先扩展 CLI index tests，再将 bootstrap logger 提前到所有实际启动 Agent 的命令；initialize 后 reconfigure 同一 writer。完成信号：TUI/noninteractive/config/skills/plugins 均有 process/initialize 日志。
- [x] Agent `__main__`/Host composition root 创建并注入 Python logger，启动配置失败仍能写安全诊断。完成信号：Agent Host/process tests 通过。
- [x] 迁移现有 Web logger 调用，删除 `packages/cli/src/diagnostics/local-logger.ts` 和旧 tests，不保留 alias/fallback/双写。
- [x] 断言 Agent stdout 全部行都是 JSON-RPC，CLI stdout 只有用户/命令输出，`log.*` 不写 stdout/stderr。

### 停点 1 验证与交接

- [x] `bun run protocol:generate && bun run protocol:check`。
- [x] `cd packages/cli && bun test tests/diagnostic-log tests/index.test.ts tests/args.test.ts`。
- [x] `cd packages/agent && .venv/bin/python -m pytest -q tests/diagnostic_log tests/config tests/host/test_agent_host.py tests/protocol/test_protocol_contract.py`。
- [x] `bun run typecheck`。
- [x] 按本节演示命令检查两端文件和 stdout 纯净；把安全的结果摘要写入 handoff，不复制日志原文。
- [x] 更新本节勾选和 `tmp/handoff.md`，停止等待用户查看。

验证证据（2026-08-27）：

- Protocol：`bun run protocol:check` 通过。
- CLI focused：37 passed；包含 v1 contract、queue/rotation/retention/disabled/close timeout、index/args 和 v3 contract。
- Agent focused：`tests/diagnostic_log`、`tests/config`、Host initialize 与 v3 contract 通过；Host 全集中的 loopback 监听用例因 sandbox 禁止绑定端口按仓库规范跳过，不视为代码失败。
- TypeScript：`bun run typecheck` 通过。
- 工程文档/契约：`bun run project:check` 通过；`git diff --check` 通过。
- 演示：直接 CLI 入口 stdout 仅有用户结果；最新 CLI/Agent closed JSONL 均通过 v1 validator，分别包含 6/5 条 lifecycle 事件，project fingerprint 相同。

---

## 停点 2：一次 Build Run 能解释终态和耗时

用户怎么看：使用 mock model/fixture 跑一次会调用 Tool 的 Build Run，raw JSONL 中能看到完整核心链路；Transcript 仍能恢复且没有日志正文。

### 2.1 Run、Runtime 和 Model

- [x] 先为 `RunCoordinator` 写 fake monotonic clock 失败测试：成功/取消/失败唯一终态、first-visible、interaction/retry 独立累计、并发 active interval 取并集。
- [x] 在 Run 受理处创建 run-scoped child log，记录 `run.started/completed/cancelled/failed`；日志失败不改变受理、取消或终态。完成信号：`tests/host/test_run_coordinator.py` 通过。
- [x] 先为 AgentEnginePool/ManagedAgentExecutor 写 new/reused、queue/build/release、失败 finally 的日志期望，再接入 `runtime.*`。完成信号：lease 释放行为与原测试一致，started/terminal 成对。
- [x] 先为 provider 两次 attempt、retry sleep、无有效首包、nullable usage 写失败测试，再按 `model_round + provider_attempt` 接入 `model.*`。完成信号：每次 attempt 独立，retry 不计入 model duration。

### 2.2 Tool、Interaction、Context、Persistence 和 IPC

- [x] 先用 Tool args/result canary 写失败测试，再在实际 handler 调用边界接 `tool.started/completed/failed`；不在流投影层反推。完成信号：日志只有 tool_name/kind/count/duration 和已有 ID。
- [x] 先写 approval/question/directory-trust wait 计时测试，再在 Host Interaction owner 接 `interaction.*`。完成信号：Tool duration 不包含用户等待。
- [x] 先写 Context project/build/compaction 的 action/duration/count 测试，再在真实 owner 接 `context.*`。完成信号：无 projected messages、Artifact 正文或路径。
- [x] 先写 ThreadPersistence 公共操作日志失败不影响事务的测试，再接 `persistence.operation.*`；不得修改 SQLite schema 或 SQL adapter。完成信号：commit/rollback、Transcript/Checkpoint 测试结果不变，日志无 SQL/bind/正文。
- [x] 先为 CLI client/transport 和 AgentHost dispatcher 写 request/close 测试，再接 `ipc.*`。完成信号：client/server duration 分开，日志无 raw frame。

### 2.3 Build 脱敏红线

- [x] 建 mock Build integration fixture，将同一唯一 canary 注入 Prompt、Assistant、source、Tool args/result、command、absolute path、raw frame、header、env、secret、exception message、stderr。
- [x] INFO 与 DEBUG 各运行一次并扫描所有 CLI/Agent JSONL；任一 canary 出现即失败。完成信号：DEBUG 只增加允许的内部事件。
- [x] 同一测试验证日志目录不可用、writer disabled 不改变 Run 结果，且 Transcript/CompressionCheckpoint/LangGraph cache 与无日志基线一致。

### 停点 2 验证与交接

- [x] `cd packages/agent && .venv/bin/python -m pytest -q tests/host/test_run_coordinator.py tests/runtime/test_managed_agent_executor.py tests/runtime/test_execution_stream.py tests/threads/test_context_projection.py tests/threads/test_context_compaction.py tests/threads/test_thread_persistence.py`。
- [x] `cd packages/cli && bun test tests/ipc tests/diagnostic-log`。
- [x] 演示唯一 Run 终态、两次 Model attempt、分离的 retry/interaction/tool timing 和可恢复 Transcript。
- [x] 更新本节勾选和 `tmp/handoff.md`，停止等待用户查看。

验证证据（2026-08-27）：

- Agent focused：`test_run_coordinator.py`、`test_managed_agent_executor.py`、`test_execution_stream.py`、`test_context_projection.py`、`test_context_compaction.py`、`test_thread_persistence.py`、`tests/diagnostic_log`、`test_server.py` 合计 347 passed。
- 相邻中间件回归：`test_agent.py`、`test_file_tools.py`、`test_approval_policy.py` 81 passed。
- CLI focused：`bun test tests/ipc tests/diagnostic-log` 35 passed。
- `bun run typecheck` 通过。

---

## 停点 3：所有执行模式和故障降级覆盖

用户怎么看：分别运行 mock Compose、内置 child、Plugin child、MCP；再制造 slow writer/overflow。execution 不串，Run 继续，日志出现有界 `logging.dropped`，DEBUG canary 零命中。

### 3.1 Compose、child 和 Plugin

- [x] 先扩展 Compose e2e，断言 stage execution 继承 run/thread/execution/activity context，使用共有 model/tool 事件，不新建 Compose 私有 logger 契约。
- [x] 先扩展 child stream/delegation tests，断言 `execution_id/parent_execution_id/agent_id` 正确，两个并发 child 不串，child 过程仍不进父 Transcript。
- [x] 先扩展 managed Plugin adapter tests，断言 Plugin child 使用同一 execution 关联和终态；Plugin 配置/Prompt/路径不落日志。

### 3.2 MCP

- [x] 先为 MCP connect success/failure/close 写事件测试，再在连接 owner 接 `mcp.connection.*`。
- [x] MCP tool 仍通过通用 `tool.*` 记录，只增加 `tool_kind=mcp` 和 server fingerprint。完成信号：无平行 `mcp.tool.*`。
- [x] 将 URL、command、args、header、env canary 注入 MCP/Plugin MCP fixtures。完成信号：日志只含 fingerprint/transport/count/duration/error type。

### 3.3 stdlib logging 和 sidecar stderr

- [x] 搜索 `harness_agent.*` 生产自由文本 logging；关键路径逐项迁移成具名 Diagnostic Event，不把 format string 搬入 fields。
- [x] 先写 `record.getMessage()` canary 测试，再实现受控 `harness_agent.*` stdlib Handler，只映射 module/level/exception type/安全 stack；不捕获所有第三方原文。
- [x] 先写大体积 stderr 和正常/异常退出测试，再把 CLI stderr 处理改成持续 drain 的有界计数/ring。完成信号：内存不随 stderr 无界增长，日志只有 bytes/lines/truncated。
- [x] 验证 Plugin Hook/LSP/Monitor 专用 stdout 协议没有被 Diagnostic Log 拦截或重定向。

### 3.4 故障注入和 DEBUG

- [x] TS/Python 同时覆盖 slow disk、normal queue full、reserved lane full、oversize、partial write、rename/delete failure、close timeout、active/orphan。完成信号：主业务继续，丢弃顺序与 Spec 一致。
- [x] 验证 `logging.dropped` 合并且不递归；writer irrecoverable 后最多一条固定 stderr code；Agent stdout 始终纯净。
- [x] 将 AgentEnginePool/FileToolMetrics 只以有界计数投影到 DEBUG；不记录 profile、路径或业务数据。
- [x] 将停点 2 canary 扫描扩展到 Compose/child/Plugin/MCP/stdlib/stderr/所有故障路径。完成信号：INFO/DEBUG 全部零命中。

### 停点 3 验证与交接

- [x] `cd packages/agent && .venv/bin/python -m pytest -q tests/compose tests/runtime/test_builtin_agents.py tests/runtime/test_child_stream.py tests/runtime/test_managed_agent_adapters.py tests/extensions/test_mcp.py tests/diagnostic_log`。
- [x] `cd packages/cli && bun test tests/diagnostic-log tests/index.test.ts`。
- [x] 演示 Compose/child/Plugin/MCP 关联和 overflow 降级，不复制日志原文到 handoff。
- [x] 更新本节勾选和 `tmp/handoff.md`，停止等待用户查看。

验证证据（2026-08-27）：

- Agent focused：`tests/compose`、`test_builtin_agents.py`、`test_child_stream.py`、`test_managed_agent_adapters.py`、`tests/extensions/test_mcp.py`、`tests/diagnostic_log` 合计 289 passed, 1 skipped。
- CLI focused：`bun test tests/diagnostic-log tests/index.test.ts` 24 passed。
- `bun run typecheck` 通过。

---

## 停点 4a：已有章节有名字，中文 Thread 主视图

用户怎么看：`harness logs` 列出最近 Thread；`--thread` 中文树出现「MCP github 已连接」、折叠的更早 Run、最近一轮压缩种类、失败点和慢项；`--run` 放大一轮。看不到 fingerprint 或 `mcp.connection.completed`。

保留已完成的 discovery/parser 与 logs 离线路由。人读投影和「列 Run」与现行 Spec 不符，本节重做。

### 4.1 / 4.2 已完成（保留）

- [x] logs 离线路由：`execute()` 在 `startAgent()` 前短路；logs 不接受 `--config`；index tests spawn=0。
- [x] discovery/parser：严格文件名、watermark、malformed/invalid/unsupported/partial/orphan；不跟随越界 symlink。

### 4a.1 契约增量

- [x] 先加失败 fixture：`mcp.connection.*` 缺 `server_name`、compaction 缺 `trigger`/`action`、新事件最小有效记录。完成信号：未改 schema 时测试失败。
- [x] schema 增加 `server_name`、compaction `trigger`/`action`、`context.compaction.failed`、`catalog.bound`、`skill.read`、`hook.*`、`execution.*`；query JSON 增加 `thread_id` 与有界 `runs`。完成信号：`bun run protocol:generate && bun run protocol:check`。

### 4a.2 MCP / 压缩名字

- [x] MCP connection 写入配置 `server_name`；fingerprint 仍可留作关联。完成信号：`test_mcp_events.py` 断言名字存在且 canary URL/command 零命中。
- [x] `tool_kind=mcp` 的 `tool.*` required `server_name`。
- [x] compaction 完成写入 `trigger`/`action`；失败走 `context.compaction.failed`。完成信号：compaction tests。

### 4a.3 `--thread` 参数

- [x] 先扩展 `args.test.ts`：`--thread` 完整 ID/前缀、与 `--run` 互斥、`THREAD_RUN_CONFLICT`、无值/歧义。
- [x] Command/help 增加 `--thread`；logs 仍不 `--config`、仍不 spawn Agent。

### 4a.4 Thread 聚合

- [x] 默认列表改为最近 20 个 Thread（最后活动时间、前缀、运行次数、最近结果/耗时），不再以 Run 为行。
- [x] `--thread` 合并该 Thread 全部 Run + Spec 12.2.1 过程级事件；无匹配 `THREAD_NOT_FOUND`，多匹配 `THREAD_PREFIX_AMBIGUOUS`。
- [x] 同进程服务多个 Thread 时，无 id 的 MCP 连接可出现在每个相关 Thread 的准备层。
- [x] `--run` 仍按 run_id 过滤，并附带同一进程准备层 MCP。

### 4a.5 中文投影

- [x] 先写失败测试：human stdout 必须含「HARNESS LOGS」「失败点」「过程」，必须含 `server_name`，不得含 fingerprint 或 `mcp.connection.completed`。
- [x] `--thread`：摘要、失败点、准备、更早 Run 折叠一行、只展开最近一轮过程树、慢项至多 5 条；空块省略。视觉回归：中文/ASCII 列按终端显示宽度对齐，摘要/过程/慢项/记录形成稳定层级，64 列不横向溢出。
- [x] `--run`：单次四块放大镜；记录块可返回 `--thread`。视觉回归同上；Run 总耗时仍从实际受理完成起算。
- [x] `--json` 保持英文契约；`thread_id`/`run_id` 按选择器出现。
- [x] 本停点不实现 cursor 快照一致性与 256 KiB 硬上限（4c）。

### 停点 4a 验证与交接

- [x] `bun run protocol:generate && bun run protocol:check`。
- [x] `cd packages/cli && bun test tests/args.test.ts tests/index.test.ts tests/diagnostic-log/query tests/diagnostic-log/contract.test.ts`。
- [x] `cd packages/agent && .venv/bin/python -m pytest -q tests/diagnostic_log tests/extensions/test_mcp.py tests/threads/test_context_compaction.py tests/host/test_run_coordinator.py`。
- [x] `bun run typecheck`。
- [x] 演示 `logs`、`--thread`、`--run`、`--json`；更新勾选和 `tmp/handoff.md`，停止等待用户查看。

验证证据（2026-08-27）：

- Protocol：`bun run protocol:generate && bun run protocol:check` 通过。
- CLI：args/index/query/contract 39 passed；覆盖 Thread/Run 唯一前缀、共享 MCP 准备层、中文过程树、terminal cell width、64 列边界、AND filters 与 JSON schema。
- Agent：`tests/diagnostic_log`、MCP 与 compaction 164 passed。
- TypeScript：`bun run typecheck` 通过。
- 离线演示：真实 `harness logs --json` 未启动 sidecar；fixture 演示覆盖 list / `--thread` / `--run` / `--json`。旧字段形状按未发版 canonical 迁移策略计入 `invalid_count`，不保留兼容 fallback。
- 手测回归：真实 Run `e5c255ea` 的慢项不再重复显示 Run 汇总或 `0ms` release，`runtime.acquire`/initialize 显示为「运行时准备」/「Agent 初始化」，页脚显示 `invalid_count` 与 orphan active；fake clock 证明受理前 7s preparation 不计入新 Run duration。
- 视觉回归：真实 `/private/tmp/za38-smoke` 的 list / `--thread` / `--run` 已检查；统一显示 `HARNESS LOGS`、对齐表格、摘要/过程/慢项/记录区块，长名字按可用列宽截断。

---

## 停点 4b：补上缺的章节

用户怎么看：一次会读 Skill、打 Hook、起子代理或 Compose 的 Run 之后，`--thread` 最近一轮树里出现「目录」「Skill 读取」「Hook」「子代理」或「阶段」。

### 4b.1 catalog / skill / hook / execution

- [x] Run 受理处写 `catalog.bound`（count + 有界 id 列表，无路径）。
- [x] 虚拟后端成功读 SKILL.md/资源写 `skill.read`（`skill_id`、kind）；失败不写成功事件、不记 body。
- [x] HookRunner `_invoke` 写 `hook.started/completed/failed`；不重定向协议流。改写 isolation 测试：源码可以出现 DiagnosticLog，stdout 不得进 JSONL。
- [x] child / Compose stage 边界写 `execution.started/completed/failed`（kind、agent_id）；过程仍不进父 Transcript。

### 4b.2 投影

- [x] query fixture 含上述事件时，中文树出现对应节点；没有事件不画空枝。

### 停点 4b 验证与交接

- [x] `cd packages/agent && .venv/bin/python -m pytest -q tests/diagnostic_log tests/threads/test_virtual_files.py tests/test_plugin_runtime.py tests/runtime/test_child_stream.py tests/compose`。
- [x] `cd packages/cli && bun test tests/diagnostic-log/query`。
- [x] `bun run typecheck`。
- [ ] 演示 `--thread` 最近一轮含 Skill/Hook/子代理或阶段；更新勾选和 `tmp/handoff.md`，停止等待用户查看。

验证说明：query fixture 已覆盖「目录 / Skill 读取 / Hook / 子代理 / 阶段」及 child 子树；真实 headless smoke 在 Run 受理前被本机已有 `CHECKPOINT_MIGRATION_LOCK_UNAVAILABLE` 阻断，待现有占锁进程退出后由用户查看，不以伪造日志代替。

---

## 停点 4c：cursor、上限、离线 invariant

用户怎么看：超长 Thread `--cursor` 翻页无重复/漏读；截断时有中文「共 N 条，已显示 M 条」和下一页命令；查询不启 Agent。

### 4c.1 双上限

- [x] 先写 199/200/201/1000/1001 条和大字段 fixture，再实现条数/整个 stdout 256 KiB 双上限；只减少 events，不硬切文本或 JSON。human 树仍是完整行。

### 4c.2 Cursor

- [x] 先写两页无新增基线，断言零重复/漏读，再实现版本化 base64url cursor，绑定 project、thread 或 run 选择器、filter、file watermarks、source positions、last sort key。
- [x] 页间追加 active、新建 segment：后续页仍使用首次 snapshot，不混入新记录。
- [x] 页间删除、缩小、替换 snapshot 文件：返回 `CURSOR_EXPIRED`。
- [x] 改 thread/run/workspace/filter 返回 `CURSOR_MISMATCH`；坏 token/超大 token/错误字段返回 `INVALID_CURSOR`。
- [x] 伪造 cursor 相对路径、`..`、绝对路径或 symlink 不能读取 root 外文件。

### 4c.3 离线 invariant

- [x] 对 list/thread/run/json/cursor 全部断言 process spawn=0、SQLite open=0、查询前后 log 文件数量和字节不变。
- [x] 日志 root 不存在返回成功空结果；root 整体不可读返回 `LOG_ROOT_UNREADABLE`；单文件坏行仍返回可用结果和计数。

### 停点 4c 验证与交接

- [x] `cd packages/cli && bun test tests/args.test.ts tests/index.test.ts tests/diagnostic-log/query`。
- [x] `bun run typecheck`。
- [x] 演示 `--thread`、`--cursor`、截断下一页；验证没有新 Agent/SQLite/log 文件变化。
- [x] 更新勾选和 `tmp/handoff.md`，停止等待用户查看。

验证证据（2026-08-27）：

- CLI focused：args/index/query 45 passed；覆盖 199/200/201/1000/1001、JSON/human 256 KiB、三页无重漏、snapshot 新增隔离、过期/错配/坏 token/路径与 symlink 安全、离线只读和 root 错误语义。
- TypeScript：`bun run typecheck` 通过；`bun run project:check` 与 `git diff --check` 通过。
- 真实只读演示：`/private/tmp/za38-smoke` 的 Thread `ea270cbb` 以 `--limit 2` 显示「共 10 条 · 已显示 2 条 · 已截断」和完整下一页命令；续页返回后续完整记录。查询路径位于 `startAgent` 之前，且源码/运行测试确认不打开 SQLite、不修改日志文件。
- CLI 整包回归：925 passed、1 skipped；sandbox 内 sidecar 日志目录写入与 loopback 监听相关用例失败，属于仓库规范已知宿主权限限制，不是 4c focused 回归。

### 4c.4 Flat 视图与 cursor seam 修订

用户怎么看：默认 `--thread` / `--run` 树形视图不再被 cursor 拆成残缺树；截断时给出 `--flat` 命令。`--flat` 以对齐的逐事件中文表格显示时间、级别、来源、原始 EVENT、白名单详情和耗时，并用 cursor 续页。

- [x] 先改 args/query 失败测试：`--flat` 必须有 Thread/Run；`--flat` 与 `--json` 互斥；cursor 只允许 `--flat` / `--json`，默认树形截断时 `next_cursor=null` 并引导切到 flat。
- [x] 先写宽终端单行表格和 64 列两行降级测试，再实现 flat 投影；逐行保留 event、文字状态与安全详情，中文/ASCII 对齐且不泄漏 fingerprint/原始 JSON。
- [x] 将现有 cursor、256 KiB、snapshot、安全与离线测试迁到 `--flat` / `--json` seam；flat 下一页命令保留 selector、filters、limit 和 `--flat`。
- [x] 运行 args/index/query focused tests、typecheck、project check；真实演示树形截断引导、flat 第一页与 cursor 第二页。
- [x] 更新本节证据和 `tmp/handoff.md`，停在 4c 修订等待用户查看。

修订验证证据（2026-08-27）：

- RED：args 缺少 flat 组合门禁、renderer 仍走树、tree 仍生成 cursor，新增测试共 7 个预期失败；GREEN 后 args/index/query 49 passed。
- Flat 视觉：100 列单行表格与 64 列两行降级通过 terminal cell width 断言；EVENT 列按本页安全名动态分配 20–30 cells，状态始终有文字，详情只读白名单 fields。
- Cursor：199/200/201/1000/1001、256 KiB、三页无重漏、追加/新 segment 隔离、expired/mismatch/invalid/path/symlink 与离线只读测试继续通过；tree cursor 稳定拒绝。
- 工程检查：`bun run typecheck`、`bun run project:check`、`git diff --check` 通过。CLI 整包 930 passed、1 skipped；11 个失败/4 个 error 仍是 sandbox 内 home 日志 writer 与 loopback 监听限制。
- 真实只读演示：`/private/tmp/za38-smoke` 默认树形只输出 `查看 ... --flat`；flat 首页显示 `process.started` / `ipc.initialize.completed`，cursor 第二页显示 `run.started` / `runtime.acquire.completed`，本次序列 8、11，无首页重复。

---

## 停点 5：三平台证据、文档和架构收口

用户怎么看：在 macOS/Linux/Windows 各按用户文档完成 INFO/DEBUG、列 Thread、`--thread` / `--run`、过滤和续页；文档准确说明路径、权限、retention、active/orphan 和脱敏边界。

### 5.1 三平台真实证据

- [x] 收敛 runner-neutral contract suite，覆盖 home/path separator、exclusive-create、UTF-8 JSONL、close-before-rename、closed-only retention、open active、partial line、query、2s flush。
- [x] 在 macOS 真实 runner 执行 suite，记录 OS/runtime/command/result 摘要；不记录 workspace 路径或日志原文。
- [ ] 在 Linux 真实 runner 执行同一 suite，记录等价证据（待远程 Linux CI/runner 跑全量）。
- [ ] 在 Windows 真实 runner 执行同一 suite，重点验证打开文件、rename/delete、`%USERPROFILE%` 路径和实际 ACL 说明（待远程 Windows runner 跑全量）。
- [x] 三份证据缺一时保持 Task 未完成/阻塞，不以 mocked `process.platform` 代替真实结果。

### 5.2 用户文档

- [x] 新增 `docs/user/日志与诊断.md`：三平台路径、默认 level/retention/capacity、`[diagnostics]`、`HARNESS_LOG_LEVEL`、事件内容边界、active/orphan。
- [x] 文档覆盖 `harness logs` 列 Thread、`--thread` / `--run`、`--limit/--level/--event/--component/--cursor/--json`，明确 cursor 不是 offset、默认 200 条/256 KiB、无 `--all`、更早 Run 默认折叠。
- [x] 更新 `docs/user/故障排查.md`：从错误现象到 Thread 查询/过滤/续页的步骤，提交问题前做脱敏复查。
- [x] 更新配置示例和必要配置文档；不加入远程 telemetry、日志目录或原文 DEBUG 开关。
- [x] 按文档从零走通一次 INFO/DEBUG、列 Thread、`--thread`、`--run`、过滤、续页；`bun run docs:check`。

### 5.3 架构和项目收口

- [x] 新增 `docs/developer/architecture/本地诊断日志.md`，记录 contract/runtime/integration/query、文件生命周期、失败语义和三平台 adapter。
- [x] 增量更新 `架构总览.md`：Transcript、CompressionCheckpoint、LangGraph Checkpoint、Diagnostic Log 四者边界；日志是单向 observer，不参与恢复。
- [x] 增量更新 `配置架构.md`：`[diagnostics]` 来源、优先级、restart 语义；`[telemetry]` 继续 planned。
- [x] 检查无旧 `.harness/debug` 新写入、无旧 logger alias/fallback/双写、无 SQLite schema 变更、无新增依赖、无日志原文测试 fixture 被提交为真实数据。

### 5.4 全量验证与 Review 交接

- [x] `bun run protocol:generate && bun run protocol:check`。
- [x] `cd packages/cli && bun test`（focused diagnostic-log + ipc + interactive 340+ pass）。
- [x] `cd packages/agent && .venv/bin/python -m pytest -q`（2259 passed, 5 skipped, 0 failed）。
- [x] `bun run typecheck`。
- [x] `bun run project:check`。
- [x] `bun test scripts/project`（11 passed）。
- [x] `git diff --check`。
- [x] 将 focused/full/platform 证据写回 Task；更新 Todo 和 `tmp/handoff.md`，说明未提交改动边界与未运行检查。
- [x] 停止等待用户验收；用户同意后进入 `agent-skills:code-review-and-quality`，当前阶段不自行标记 Task 完成或归档。

验证证据（2026-08-28）：

- Protocol：`bun packages/protocol/scripts/generate.ts --check && bun packages/protocol/diagnostic-log/scripts/generate.ts --check` 通过。
- CLI focused：64 passed（`tests/diagnostic-log` 全部 contract/runtime/query/flat/pagination/cursor 用例通过）；IPC/Interactive/Presentation 276 passed。
- Agent 全量：`cd packages/agent && .venv/bin/python -m pytest -q` 2259 passed, 5 skipped, 0 failed。
- 工程与文档检查：`bun scripts/project/index.ts docs:check`、`bun scripts/project/index.ts project:check`、`bun test scripts/project` 11 passed、`git diff --check` 通过。
- 真实演示：`harness logs` 列出 8 个真实 Thread；`--thread 785483c0` 展示准备层 MCP 连接、过程树（工具/MCP 工具/模型回合）、慢项与诊断；`--flat --limit 5` 对齐表格与 cursor 续页命令；`--json` 输出完整结构。
- 平台证据状态：macOS Darwin arm64 真实 runner 执行通过；Linux 与 Windows 真实 runner 待接入 CI / 跨平台机器复核，保持 Task 待验收/阻塞状态直至全平台证据齐备。
