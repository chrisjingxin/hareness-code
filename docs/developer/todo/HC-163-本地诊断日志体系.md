# HC-163 本地诊断日志体系执行清单

关联：[Task](../task/HC-163-本地诊断日志体系.md) · [Spec](../spec/HC-163-本地诊断日志体系.md) · [Plan](../plan/HC-163-本地诊断日志体系.md)

每项行为变更先写最小失败测试，再写最小实现。一个停点做完后：勾选本节、记录命令与结果、更新 `tmp/handoff.md`，然后停下等用户查看。

---

## 停点 1：两个进程都能安全异步落日志

用户怎么看：

```bash
HARNESS_LOG_LEVEL=debug bun run dev -- --non-interactive --message "只回复 ok"
```

查看 `.harness/logs/<date>/`：有 CLI/Agent 独立 JSONL，含 logging/process/initialize 事件；stdout 只有 `ok`。

### 1.1 Diagnostic Log v1 contract

- [x] 先在 Protocol tests 加失败用例：最小有效 envelope、每事件 fields、`additionalProperties=false`、level/event 不匹配、8 KiB 上限、NaN/Infinity、敏感 key。完成信号：未实现 v1 schema 时测试稳定失败。
- [x] 新增 `packages/protocol/diagnostic-log/schema/v1.json` 与事件/query schema；envelope、事件目录、级别和字段与 Spec 一致。完成信号：所有正反 fixture 符合预期。
- [x] 扩展 generator，生成 TypeScript 类型/validator/digest 和 Python generated contract/validator/digest；不手写双份事件目录。完成信号：`bun run protocol:generate && bun run protocol:check`。
- [x] 增加 `@za38/protocol/diagnostic-log` export 和 TS↔Python 互验 fixture。完成信号：任一端产生的 v1 记录被另一端接受，多字段/错类型两端一致拒绝。

### 1.2 TypeScript runtime

- [x] 先在 `packages/cli/tests/diagnostic-log/runtime/` 写 queue 失败测试：4096 条/8 MiB 双上限、128 条/256 KiB WARN/ERROR 保留、DEBUG → INFO 淘汰、record sequence 不复用。
- [x] 实现 TS `DiagnosticLog.child/debug/info/warn/error` 与 immutable context；fields 不能覆盖 envelope ID。完成信号：调用点不需要组 JSON/envelope。
- [x] 实现内存校验/脱敏/限长/编码与有界入队；contract violation/oversize 不抛回业务路径。完成信号：反例只增计数并合并为安全事件。
- [x] 先写 slow/failing writer、partial write、close timeout 失败测试，再实现单后台 writer 和最多 2 秒 close。完成信号：`log.*` 不 await 磁盘，writer 失败后 disabled 且不影响主流程。
- [x] 先写 path/rotation/retention 失败测试，再实现平台 home API、exclusive-create、close-before-rename、closed-only retention、active/orphan 不动。完成信号：`cd packages/cli && bun test tests/diagnostic-log/runtime`。
- [x] 删除 TS 生产日志路径中的同步文件 I/O。完成信号：搜索无任何日志热路径 `writeSync`。

### 1.3 Python runtime

- [x] 先在 `packages/agent/tests/diagnostic_log/` 复制同一 queue/file lifecycle 行为表的失败测试，使用 fake clock/home/writer。
- [x] 实现 Python `DiagnosticLog.child/debug/info/warn/error` 和对等 validator/context 语义。完成信号：TS/Python 对同一 fixture 给出同一接受/拒绝结果。
- [x] 实现单后台 worker、有界 queue、batch append、rotation/retention、disabled 降级和 2 秒 close。完成信号：asyncio Run 在 slow writer 下继续，丢弃策略与 TS 一致。
- [x] 验证 POSIX 目录 `0700`/文件 `0600`，Windows adapter 不把 mode bit 当 ACL 保证。完成信号：`cd packages/agent && .venv/bin/python -m pytest -q tests/diagnostic_log`。

### 1.4 配置、Protocol 和启动接入

- [x] 先为 `[diagnostics]` 写配置失败测试：level 枚举、retention/capacity 范围、`max_file_mib <= max_total_mib`、`HARNESS_LOG_LEVEL` 优先、`applies_to=restart`。
- [x] ConfigManifest/Config/ConfigChangeService 实现 `[diagnostics]`，不激活 `[telemetry]`。完成信号：`tests/config` focused tests 通过。
- [x] 先修改 JSON-RPC contract tests，再给 `InitializeResult` 加 required effective diagnostics；生成 TS/Python 两端。完成信号：`bun run protocol:generate && bun run protocol:check`。
- [x] 修正所有命令的 `connection.project.id`：不打开 Thread SQLite 也返回真实 workspace fingerprint，不再返回 `echo`。完成信号：管理命令测试断言 SQLite 未打开但 project id 正确。
- [x] 先扩展 CLI index tests，再将 bootstrap logger 提前到所有实际启动 Agent 的命令；initialize 后 reconfigure 同一 writer。完成信号：TUI/noninteractive/config/skills/plugins 均有 process/initialize 日志。
- [x] Agent `__main__`/Host composition root 创建并注入 Python logger，启动配置失败仍能写安全诊断。完成信号：Agent Host/process tests 通过。
- [x] 迁移现有 Web logger 调用，删除 `packages/cli/src/diagnostics/local-logger.ts` 和旧 tests，不保留 alias/fallback/双写。
- [x] 断言 Agent stdout 全部行都是 JSON-RPC，CLI stdout 只有用户/命令输出，`log.*` 不写 stdout/stderr。

### 停点 1 验证与交接

- [x] `bun run protocol:generate && bun run protocol:check`。
- [x] `cd packages/cli && bun test tests/diagnostic-log tests/index.test.ts tests/args.test.ts`。
- [x] `cd packages/agent && .venv/bin/python -m pytest -q tests/diagnostic_log tests/config tests/host/test_agent_host.py tests/protocol/test_protocol_contract.py`。
- [x] `bun run typecheck`。
- [x] 按本节演示命令检查两端文件和 stdout 纯净；把安全的结果摘要写入 handoff，不复制日志原文。
- [x] 更新本节勾选和 `tmp/handoff.md`，停止等待用户查看。

验证证据（2026-08-27）：

- Protocol：`bun run protocol:check` 通过。
- CLI focused：37 passed；包含 v1 contract、queue/rotation/retention/disabled/close timeout、index/args 和 v3 contract。
- Agent focused：`tests/diagnostic_log`、`tests/config`、Host initialize 与 v3 contract 通过；Host 全集中的 loopback 监听用例因 sandbox 禁止绑定端口按仓库规范跳过，不视为代码失败。
- TypeScript：`bun run typecheck` 通过。
- 演示：直接 CLI 入口 stdout 仅有用户结果；最新 CLI/Agent closed JSONL 均通过 v1 validator，分别包含 6/5 条 lifecycle 事件，project fingerprint 相同。

---

## 停点 2：一次 Build Run 能解释终态和耗时

用户怎么看：使用 mock model/fixture 跑一次会调用 Tool 的 Build Run，raw JSONL 中能看到完整核心链路；Transcript 仍能恢复且没有日志正文。

### 2.1 Run、Runtime 和 Model

- [ ] 先为 `RunCoordinator` 写 fake monotonic clock 失败测试：成功/取消/失败唯一终态、first-visible、interaction/retry 独立累计、并发 active interval 取并集。
- [ ] 在 Run 受理处创建 run-scoped child log，记录 `run.started/completed/cancelled/failed`；日志失败不改变受理、取消或终态。完成信号：`tests/host/test_run_coordinator.py` 通过。
- [ ] 先为 AgentEnginePool/ManagedAgentExecutor 写 new/reused、queue/build/release、失败 finally 的日志期望，再接入 `runtime.*`。完成信号：lease 释放行为与原测试一致，started/terminal 成对。
- [ ] 先为 provider 两次 attempt、retry sleep、无有效首包、nullable usage 写失败测试，再按 `model_round + provider_attempt` 接入 `model.*`。完成信号：每次 attempt 独立，retry 不计入 model duration。

### 2.2 Tool、Interaction、Context、Persistence 和 IPC

- [ ] 先用 Tool args/result canary 写失败测试，再在实际 handler 调用边界接 `tool.started/completed/failed`；不在流投影层反推。完成信号：日志只有 tool_name/kind/count/duration 和已有 ID。
- [ ] 先写 approval/question/directory-trust wait 计时测试，再在 Host Interaction owner 接 `interaction.*`。完成信号：Tool duration 不包含用户等待。
- [ ] 先写 Context project/build/compaction 的 action/duration/count 测试，再在真实 owner 接 `context.*`。完成信号：无 projected messages、Artifact 正文或路径。
- [ ] 先写 ThreadPersistence 公共操作日志失败不影响事务的测试，再接 `persistence.operation.*`；不得修改 SQLite schema 或 SQL adapter。完成信号：commit/rollback、Transcript/Checkpoint 测试结果不变，日志无 SQL/bind/正文。
- [ ] 先为 CLI client/transport 和 AgentHost dispatcher 写 request/close 测试，再接 `ipc.*`。完成信号：client/server duration 分开，日志无 raw frame。

### 2.3 Build 脱敏红线

- [ ] 建 mock Build integration fixture，将同一唯一 canary 注入 Prompt、Assistant、source、Tool args/result、command、absolute path、raw frame、header、env、secret、exception message、stderr。
- [ ] INFO 与 DEBUG 各运行一次并扫描所有 CLI/Agent JSONL；任一 canary 出现即失败。完成信号：DEBUG 只增加允许的内部事件。
- [ ] 同一测试验证日志目录不可用、writer disabled 不改变 Run 结果，且 Transcript/CompressionCheckpoint/LangGraph cache 与无日志基线一致。

### 停点 2 验证与交接

- [ ] `cd packages/agent && .venv/bin/python -m pytest -q tests/host/test_run_coordinator.py tests/runtime/test_managed_agent_executor.py tests/runtime/test_execution_stream.py tests/threads/test_context_projection.py tests/threads/test_context_compaction.py tests/threads/test_thread_persistence.py`。
- [ ] `cd packages/cli && bun test tests/ipc tests/diagnostic-log`。
- [ ] 演示唯一 Run 终态、两次 Model attempt、分离的 retry/interaction/tool timing 和可恢复 Transcript。
- [ ] 更新本节勾选和 `tmp/handoff.md`，停止等待用户查看。

---

## 停点 3：所有执行模式和故障降级覆盖

用户怎么看：分别运行 mock Compose、内置 child、Plugin child、MCP；再制造 slow writer/overflow。execution 不串，Run 继续，日志出现有界 `logging.dropped`，DEBUG canary 零命中。

### 3.1 Compose、child 和 Plugin

- [ ] 先扩展 Compose e2e，断言 stage execution 继承 run/thread/execution/activity context，使用共有 model/tool 事件，不新建 Compose 私有 logger 契约。
- [ ] 先扩展 child stream/delegation tests，断言 `execution_id/parent_execution_id/agent_id` 正确，两个并发 child 不串，child 过程仍不进父 Transcript。
- [ ] 先扩展 managed Plugin adapter tests，断言 Plugin child 使用同一 execution 关联和终态；Plugin 配置/Prompt/路径不落日志。

### 3.2 MCP

- [ ] 先为 MCP connect success/failure/close 写事件测试，再在连接 owner 接 `mcp.connection.*`。
- [ ] MCP tool 仍通过通用 `tool.*` 记录，只增加 `tool_kind=mcp` 和 server fingerprint。完成信号：无平行 `mcp.tool.*`。
- [ ] 将 URL、command、args、header、env canary 注入 MCP/Plugin MCP fixtures。完成信号：日志只含 fingerprint/transport/count/duration/error type。

### 3.3 stdlib logging 和 sidecar stderr

- [ ] 搜索 `harness_agent.*` 生产自由文本 logging；关键路径逐项迁移成具名 Diagnostic Event，不把 format string 搬入 fields。
- [ ] 先写 `record.getMessage()` canary 测试，再实现受控 `harness_agent.*` stdlib Handler，只映射 module/level/exception type/安全 stack；不捕获所有第三方原文。
- [ ] 先写大体积 stderr 和正常/异常退出测试，再把 CLI stderr 处理改成持续 drain 的有界计数/ring。完成信号：内存不随 stderr 无界增长，日志只有 bytes/lines/truncated。
- [ ] 验证 Plugin Hook/LSP/Monitor 专用 stdout 协议没有被 Diagnostic Log 拦截或重定向。

### 3.4 故障注入和 DEBUG

- [ ] TS/Python 同时覆盖 slow disk、normal queue full、reserved lane full、oversize、partial write、rename/delete failure、close timeout、active/orphan。完成信号：主业务继续，丢弃顺序与 Spec 一致。
- [ ] 验证 `logging.dropped` 合并且不递归；writer irrecoverable 后最多一条固定 stderr code；Agent stdout 始终纯净。
- [ ] 将 AgentEnginePool/FileToolMetrics 只以有界计数投影到 DEBUG；不记录 profile、路径或业务数据。
- [ ] 将停点 2 canary 扫描扩展到 Compose/child/Plugin/MCP/stdlib/stderr/所有故障路径。完成信号：INFO/DEBUG 全部零命中。

### 停点 3 验证与交接

- [ ] `cd packages/agent && .venv/bin/python -m pytest -q tests/compose tests/runtime/test_builtin_agents.py tests/runtime/test_child_stream.py tests/runtime/test_managed_agent_adapters.py tests/extensions/test_mcp.py tests/diagnostic_log`。
- [ ] `cd packages/cli && bun test tests/diagnostic-log tests/index.test.ts`。
- [ ] 演示 Compose/child/Plugin/MCP 关联和 overflow 降级，不复制日志原文到 handoff。
- [ ] 更新本节勾选和 `tmp/handoff.md`，停止等待用户查看。

---

## 停点 4：`harness logs` 完整离线查询

用户怎么看：运行 `harness logs`、`--run`、filters、`--json` 和 `--cursor`；能看到最近 Run、合并时间线、截断信息和稳定下一页。

### 4.1 参数和离线路由

- [ ] 先扩展 `args.test.ts`：logs list/run、完整 ID/前缀、level/event/component、limit 1..1000、cursor、无值/非法枚举、`--cwd`。
- [ ] 扩展 `Command` 和 help，`execute()` 在 `startAgent()` 前路由 logs query；logs 不接受 `--config`。完成信号：`index.test.ts` 中 spawn 调用为 0。

### 4.2 文件发现和解析

- [ ] 先建立严格日期目录/segment 文件名、普通文件、symlink/path traversal 的正反 fixture，再实现 root-contained discovery。
- [ ] 先建立 active 半行、坏 JSON、invalid v1、unknown schema fixture，再实现逐行 validator 和 malformed/invalid/unsupported/partial 计数；单行损坏不终止查询。
- [ ] 首次查询固定每个文件的 byte watermark，不读取随后追加内容；source 只保留受控相对路径/byte offset。
- [ ] 识别并报告 orphan active count/bytes，不自动删除或改名。

### 4.3 Run 聚合、过滤和输出

- [ ] 先建立多 workspace、CLI/Agent、多进程时钟偏差、PID 复用、同前缀 fixture，再实现当前 workspace 最近 Run 列表和唯一前缀解析。完成信号：默认最近 20，无匹配/歧义稳定报错。
- [ ] 按 Spec 排序键实现合并；进程内 sequence 稳定，跨进程只按 wall clock 展示，不据此算 duration。
- [ ] 先写 level threshold、event namespace/exact、component 和 AND 组合测试，再实现过滤。
- [ ] 先写 human timeline/慢项/失败点/counts/next command snapshot，再实现安全投影；不输出自由 message 或绝对源路径。
- [ ] 先写 query schema v1 fixture，再实现 `--json`；errors 也输出稳定版本化 object。
- [ ] 先写 199/200/201/1000/1001 条和大字段 fixture，再实现条数/整个 stdout 256 KiB 双上限；只减少 events，不硬切文本或 JSON。

### 4.4 Cursor

- [ ] 先写两页无新增基线，断言零重复/漏读，再实现版本化 base64url cursor，绑定 project/run/filter/file watermarks/source positions/last sort key。
- [ ] 页间追加 active、新建 segment：后续页仍使用首次 snapshot，不混入新记录。
- [ ] 页间删除、缩小、替换 snapshot 文件：返回 `CURSOR_EXPIRED`，不猜位置继续。
- [ ] 改 run/workspace/filter 返回 `CURSOR_MISMATCH`；坏 token/超大 token/错误字段返回 `INVALID_CURSOR`。
- [ ] 伪造 cursor 相对路径、`..`、绝对路径或 symlink 不能读取 root 外文件。

### 4.5 离线 invariant

- [ ] 对 list/run/json/cursor 全部断言 process spawn=0、SQLite open=0、查询前后 log 文件数量和字节不变。
- [ ] 日志 root 不存在返回成功空结果；root 整体不可读返回 `LOG_ROOT_UNREADABLE`；单文件坏行仍返回可用结果和计数。

### 停点 4 验证与交接

- [ ] `cd packages/cli && bun test tests/args.test.ts tests/index.test.ts tests/diagnostic-log/query`。
- [ ] `bun run typecheck`。
- [ ] 演示 `logs`、`--run`、三类 filter、JSON、截断和 cursor 下一页；验证没有新 Agent/SQLite/log 文件变化。
- [ ] 更新本节勾选和 `tmp/handoff.md`，停止等待用户查看。

---

## 停点 5：三平台证据、文档和架构收口

用户怎么看：在 macOS/Linux/Windows 各按用户文档完成 INFO/DEBUG、Run 查询、过滤和续页；文档准确说明路径、权限、retention、active/orphan 和脱敏边界。

### 5.1 三平台真实证据

- [ ] 收敛 runner-neutral contract suite，覆盖 home/path separator、exclusive-create、UTF-8 JSONL、close-before-rename、closed-only retention、open active、partial line、query、2s flush。
- [ ] 在 macOS 真实 runner 执行 suite，记录 OS/runtime/command/result 摘要；不记录 workspace 路径或日志原文。
- [ ] 在 Linux 真实 runner 执行同一 suite，记录等价证据。
- [ ] 在 Windows 真实 runner 执行同一 suite，重点验证打开文件、rename/delete、`%USERPROFILE%` 路径和实际 ACL 说明。
- [ ] 三份证据缺一时保持 Task 未完成/阻塞，不以 mocked `process.platform` 代替真实结果。

### 5.2 用户文档

- [ ] 新增 `docs/user/日志与诊断.md`：三平台路径、默认 level/retention/capacity、`[diagnostics]`、`HARNESS_LOG_LEVEL`、事件内容边界、active/orphan。
- [ ] 文档覆盖 `harness logs` list/run、`--limit/--level/--event/--component/--cursor/--json`，明确 cursor 不是 offset、默认 200 条/256 KiB、无 `--all`。
- [ ] 更新 `docs/user/故障排查.md`：从错误现象到 Run 查询/过滤/续页的步骤，提交问题前做脱敏复查。
- [ ] 更新配置示例和必要配置文档；不加入远程 telemetry、日志目录或原文 DEBUG 开关。
- [ ] 按文档从零走通一次 INFO/DEBUG、查 Run、过滤、续页；`bun run docs:check`。

### 5.3 架构和项目收口

- [ ] 新增 `docs/developer/architecture/本地诊断日志.md`，记录 contract/runtime/integration/query、文件生命周期、失败语义和三平台 adapter。
- [ ] 增量更新 `架构总览.md`：Transcript、CompressionCheckpoint、LangGraph Checkpoint、Diagnostic Log 四者边界；日志是单向 observer，不参与恢复。
- [ ] 增量更新 `配置架构.md`：`[diagnostics]` 来源、优先级、restart 语义；`[telemetry]` 继续 planned。
- [ ] 检查无旧 `.harness/debug` 新写入、无旧 logger alias/fallback/双写、无 SQLite schema 变更、无新增依赖、无日志原文测试 fixture 被提交为真实数据。

### 5.4 全量验证与 Review 交接

- [ ] `bun run protocol:generate && bun run protocol:check`。
- [ ] `cd packages/cli && bun test`。
- [ ] `cd packages/agent && .venv/bin/python -m pytest -q`。
- [ ] `bun run typecheck`。
- [ ] `bun run project:check`。
- [ ] `bun run test`。
- [ ] `git diff --check`。
- [ ] 将 focused/full/platform 证据写回 Task；更新 Todo 和 `tmp/handoff.md`，说明未提交改动边界与未运行检查。
- [ ] 停止等待用户验收；用户同意后进入 `agent-skills:code-review-and-quality`，当前阶段不自行标记 Task 完成或归档。
