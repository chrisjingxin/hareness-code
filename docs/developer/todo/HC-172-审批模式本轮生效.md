# HC-172 审批模式本轮生效执行清单

关联：[Task](../task/archive/HC-172-审批模式本轮生效.md) · [Spec](../spec/HC-172-审批模式本轮生效.md) · [Plan](../plan/HC-172-审批模式本轮生效.md)

执行约束：实现已通过独立 review 与用户验收，并由 `b9c493a` 推送至 `origin/master`；本清单保留实际验证证据，任务现按生命周期完成并归档。所有测试使用 fake model/transport，不读取真实密钥或网络。

## 阶段 1：RED 与协议

- [x] 添加 Protocol/Runtime 最小失败测试，记录未实现时的明确导入失败。
- [x] 在 `packages/protocol/schema/v3.json` 增加 `run.set_approval_mode`、`run.approval_mode` capability、mode/revision result 和 stable errors。
- [x] 运行 `bun run protocol:generate` 与 canonical `bun run protocol:check`；确认 TS/Python/fixture/digest 一致。
- [x] 添加 `ApprovalModeState`、RunPreparation 实际初始 mode、pending Interaction tracking 和 Coordinator 锁内 set seam。
- [x] 注册 Host handler、ControlLease/capability/owner/status/pending 检查与脱敏 diagnostic；补双端协议、Coordinator 和 Host tests。

## 阶段 2：CLI 活动切换（可演示停点）

- [x] Gateway/Client 实现 set RPC，result 校验 thread/run/mode/revision。
- [x] `RunFeature` 活动 Run 先 RPC 后本地提交；RPC 失败不写 override/revision；idle 仍只影响下一轮。
- [x] Controller、`/plan`、Shift+Tab、Web typed intent 全部 await 同一 Core intent；pending Interaction 仍返回 busy。
- [x] CLI focused test 覆盖活动切换、rollback、pending busy、plan restore；内存 Gateway 观察 mode/revision。
- [x] 通过内存 Gateway 演示活动 Run 变更和失败回滚；更新本 Todo、`tmp/handoff.md`，并记录活动切档停点的验收证据。

## 阶段 1/2 停点证据

- RED：`test_python_validates_active_run_approval_mode_operation` 在生成合约前因 `RunSetApprovalModeParams` 不存在而 collection 失败；`test_dynamic_approval_mode.py` 在实现前因 `ApprovalModeState` 不存在而 collection 失败。
- GREEN：协议/Runtime/Coordinator/Host focused pytest `69 passed`；CLI focused Bun tests `69 pass, 0 fail`。
- 活动演示：内存 Gateway 的 `/plan` 先记录 `run.set_approval_mode`，再把 snapshot 改为 `plan`、revision `1`；注入 RPC 失败时 snapshot 保持 `default`、revision `0`。
- 阶段 3/4 已在本轮继续完成；以下记录本轮新增的动态边界、竞态、文档和验证证据。

## 阶段 3：运行时动态边界

- [x] `RunContext` 共享 Host state/current helper；模型 prompt 和 plan constraint 使用实际 mode。
- [x] HITL 编译安全超集；AUTO classifier/destructive guard 与 workspace external-directory trust 动态启停。
- [x] Plan hard deny、一次性 `RunPlanConstraint`、classifier unavailable fail-closed、hard policy precedence 回归。
- [x] root/Inline/Managed child 使用 parent current mode 与 child maximum 求交；验证收紧/放宽、运行中不取消和 HC-168 `on_resume_consumed`。
- [x] RuntimeState/compaction capture 写实际 mode，`run.started` 保持初始，新增 mode-change structured redacted diagnostic。

## 阶段 4：收尾验证

- [x] 补 cancel/terminal race、invalid thread/run、non-owner、ControlLease、all pending Interaction busy；外部目录继续由独立 `WorkspaceBoundary`/`directory_trust` 工具边界保护，pending directory-trust Interaction 继续按 busy 处理。
- [x] 更新 `docs/user/交互使用.md`、`docs/user/安全与沙箱.md`、`docs/user/subagent.md` 和 `docs/developer/architecture/架构总览.md`。
- [x] 运行最终 focused tests、`bun run protocol:generate`/`bun run protocol:check`、`bun run typecheck`、相关 CLI Bun tests、Agent pytest、`bun run project:check`、`git diff --check`；通过项与既有宿主阻塞见下方及交接记录。
- [x] 把最终每条命令的通过/跳过/既有环境阻塞写回 Task；独立 review 与用户验收已通过，`b9c493a` 已按用户授权提交并推送，任务完成后按生命周期归档。

## 阶段 3/4 动态回归证据

- RED → GREEN：未实现时 `test_dynamic_approval_mode.py` 因 `ApprovalModeState` 缺失而 collection 失败；实现后 Runtime/Policy/Host/Thread focused 集合通过，目录边界与 pending trust Interaction 回归保持通过。
- 动态策略：`test_dynamic_approval_mode.py`、`test_approval_policy.py`、`test_workspace_boundary.py`、`test_child_approval.py` 覆盖 mode/provider、HITL 超集、Plan/AUTO/目录 trust 和 child 运行时切换。
- 子代理与静态 Policy：`test_builtin_agents.py`、`test_agent_catalog.py`、Qwen Managed stream 回归覆盖父当前档位与 child ceiling 求交，raw `never/on-risk/always` 先映射到 canonical runtime ceiling。
- 最终 focused：第二轮前既有 CLI `85 pass, 0 fail`，加入第二轮并发 set 回归后 `86 pass, 0 fail`，加入本轮并发 cycle 回归后按用户指定五文件命令 `87 pass, 0 fail`；Agent 核心 `318 passed, 1 skipped, 1 warning`；Agent 全量 `2767 passed, 6 skipped, 22 failed, 3 warnings`。全量失败属于既有 Rubric、sandbox/loopback/Plugin-lock/Qwen bare-node 环境限制；compaction focused 测试保留一个既有 `aiosqlite` event-loop-closed warning，不影响退出码。
- 根 `bun run test` 的工程脚本 `11 pass`；CLI 全量 `1105 pass, 1 skip, 5 fail`，因 sidecar/loopback 失败未进入 Agent 子命令。`bun run project:check` 仅因 HC-131 过期复核日期失败；未修改该无关任务。
- 第二轮验收补充：P1 先以同一共享 Engine、两个 thread/run、同一 `call-1` 和不同参数复现缓存串用，GREEN 后确认模型后处理、HITL 预检与执行守卫共享 Run 命名空间；reject streak 同样按 Run 隔离。P2 先以并发 yolo→plan、合法 revision 1/2 复现 Plan 退出回 default，GREEN 后改为同一 Run RPC 串行提交并恢复 yolo。
- 第三轮验收补充：P2 先以延迟首个 `run.set_approval_mode`、并发两次 `approval-mode.cycle` 复现第二次仍发送 `auto-edit` 的 RED；修复为队列任务执行时解析 cycle 目标后，第二次发送 `auto`，最终 snapshot 为 `auto`、revision 为 `2` 且 revision 单调。显式 set 队列仍保持固定目标与原有失败/终态语义。

## 第四轮验收返修

- [x] 将 Coordinator/Host 的未受信 workspace 活动切档测试改为正向 `default → auto → yolo` 回归，并记录现有 trust gate 的 RED。
- [x] 删除 active `run.set_approval_mode` 的 repository trust 门禁与专属错误映射；保留 owner、ControlLease、Run 状态、pending Interaction、Plan constraint 和执行层硬策略。
- [x] 只从 canonical schema 删除该未使用错误，运行 `bun run protocol:generate` 和 `bun run protocol:check`；不增加 `source`/`user_initiated` 字段。
- [x] 更新用户/架构文档与 handoff，完成指定 Agent Host/Coordinator、policy、CLI、typecheck、protocol、diff 回归并完成 review 与用户验收。

第四轮结果：正向 Coordinator/AgentHost 目标测试 RED 均失败，修复后 `2 passed`；Host/Coordinator 文件集合 `68 passed, 1 skipped, 9 failed`（loopback sandbox 限制），policy `138 passed, 1 skipped`；CLI 五文件 `87 pass, 0 fail`；生成/协议检查、typecheck、diff check 通过；工作树指定范围 grep 无结果。

第四轮文档 P2 结果：`additional_directories`、运行时 `WorkspaceBoundary`/`interaction.directory_trust` 与历史 `trusted_directories` registry 已明确分层；`docs:check`、`project:check` 仅被 HC-131 过期 `review_due` 阻塞，`git diff --check` 通过。
