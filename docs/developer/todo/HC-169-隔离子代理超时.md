# HC-169 隔离子代理超时执行清单

关联：[Task](../task/HC-169-隔离子代理超时.md) · [Spec](../spec/HC-169-隔离子代理超时.md) · [Plan](../plan/HC-169-隔离子代理超时.md)

执行代理不得重新决定错误范围、timeout 时长或其它调用方语义。做到本停点后更新 `tmp/handoff.md` 并停止。

## 停点 1：子代理超时，主对话继续

用户怎么看：派出卡超时后显示失败与 `DELEGATION_TIMEOUT`，随后同一主 Run 继续出现主 Agent 回复；已启动 child 的时间线仍可进入。

- [x] Red：新增执行中 timeout 测试，证明 runner 被取消、child 为 failed、slot 可复用。
- [x] Red：新增 `DelegationContextMiddleware` 测试，证明 timeout 返回 `ToolMessage(status="error")`，内容包含 `timed_out` 和 `DELEGATION_TIMEOUT`。
- [x] Red：证明非 timeout 的 `AgentDelegationError` 与父 `CancelledError` 不被转换。
- [x] Red：生产 Agent/Host fake-model 纵向测试先失败，要求 timeout 后主模型进入下一轮，最终 `run.completed` 且无 `run.failed`。
- [x] Green：只在主 `task` Middleware 边界实现精确转换；不修改 300 秒 hard deadline、runner cancel、child finalize 或 slot release。
- [x] Green：排队 timeout 走同一可恢复 ToolMessage，但不创建/绑定 child。
- [x] CLI 回归：`is_error=true` 的 task 卡显示失败与超时结论，后续主回复仍显示；TUI/Web 共享行为。
- [x] 回归：Compose/Team timeout、父取消、策略/目标/身份错误保持现状。
- [x] 文档：更新 `docs/user/subagent.md` 与 `docs/developer/architecture/架构总览.md`。
- [x] 验证：运行 Plan 中 focused tests、`bun run project:check`、`bun run typecheck`、`bun run test`、`git diff --check`；记录精确结果和环境限制。
- [x] 交接：更新 Task 的 `test_evidence`、勾选已完成项并写 `tmp/handoff.md`；保持待验收，不提交、不推送、不归档。

## 执行结果

- focused 通过：Agent delegation/runtime `39 passed`；Host timeout `1 passed`；Compose/Team `15 passed`；CLI interactive/TUI/Web/child timeline `116 passed`；`bun run typecheck` 与 `git diff --check` 通过。
- `bun run project:check` / `bun run tasks:sync`：被无关 HC-160 的 `review_due=2026-09-04` 阻塞，未修改 HC-160。
- `bun run test`：`1031 pass, 1 skip, 5 fail`；4 个为 Web loopback `EADDRINUSE`，1 个 sidecar 为 `HARNESS_DIAGNOSTIC_WRITER_FAILED` / `PLUGIN_LOCK_UNAVAILABLE`，详见 `tmp/handoff.md`。
