# HC-160 隔离延迟工具状态 执行清单

## 阶段 1：底层存储与上下文（停点 1）
- [x] 1.1 新增 `ThreadDeferredToolStore` 实现（位于 `packages/agent/harness_agent/threads/deferred_store.py`）。
- [x] 1.2 在 `packages/agent/harness_agent/runtime/run_context.py` 的 `RunContext` 中增加 `deferred_tool_store` 字段。
- [x] 1.3 编写 `ThreadDeferredToolStore` 独立单测并全部通过。

## 阶段 2：重构 `DeferredToolMiddleware` 与工具接入
- [x] 2.1 重构 `DeferredToolMiddleware`：基于 `RunContext` / `runtime` 读取 `revealed`，移除共享实例字段 `_revealed`。
- [x] 2.2 在 `DeferredToolMiddleware` 中实现 `wrap_tool_call` / `awrap_tool_call`，自动从 `tool_search` 返回结果中提取命中的工具名并写入对应 Thread。
- [x] 2.3 调整 `packages/agent/harness_agent/tools/harness_tools.py`：清理 `create_harness_tools` 中废弃的 `reveal` 闭包参数。
- [x] 2.4 更新 `test_deferred_tools.py` 单测并全部通过。

## 阶段 3：主机与运行时装配
- [x] 3.1 在 `packages/agent/harness_agent/host/agent_host.py` 中初始化 `_deferred_tool_store` 并在 `_build_run_context` 中传入 `RunContext`。
- [x] 3.2 检查 `agent.py` 中的 `create_za38_agent` / `create_harness_agent` 中对 `DeferredToolMiddleware` 的初始化并统一使用中间件拦截。

## 阶段 4：跨 Thread 隔离验证与完整测试（停点 2）
- [x] 4.1 编写 `test_defer_tools_isolated_across_multiple_threads` 测试，验证共享 AgentEngine 下 Thread A reveal 不污染 Thread B。
- [x] 4.2 验证同一 Thread 多轮对话中 reveal 工具保持可用。
- [x] 4.3 运行全量 Python 单元测试，确保无回归。
- [x] 4.4 同步更新任务看板与 Task 状态。
