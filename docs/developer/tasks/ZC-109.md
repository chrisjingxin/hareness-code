---
id: ZC-109
title: 修复旧版 PromptEpoch 数据库无法迁移
priority: P1
status: 已完成
owner: Codex
branch: codex/content-master-integration
scope: 允许经过严格 schema 校验的 v2-v6 PromptEpoch 数据库，以及合并前将 v6 表结构误标为 v7 的已知旧分支数据库，单向迁移到当前 schema，同时继续拒绝无法证明来源的旧表。
acceptance: 合法 v3 数据库和已识别的旧分支 v7 数据库可自动备份并升级到 v11，PromptEpoch 转为 legacy RunContextSnapshot 且旧表删除；当前 v7 及更高版本异常残留 PromptEpoch 仍在写入前失败关闭。
user_docs: docs/user/故障排查.md
developer_docs: docs/developer/architecture/架构总览.md、docs/developer/architecture/adr/0003-transcript-context-projection-lifecycle.md
test_evidence: ThreadPersistence 迁移专项 87 passed；旧分支 v7 临时副本升级后 integrity=ok、schema=11、PromptEpoch 表 0、Transcript/Team 数据保留；Python 非 Host full 809 passed、2 skipped、1 deselected；Host 扩展/生命周期专项 13 passed；project:check、protocol:check、typecheck、build、git diff --check passed
references: docs/developer/tasks/ZC-107.md、docs/developer/tasks/ZC-108.md
completed_at: 2026-08-04
---

## 背景

当前迁移器能够严格识别 schema v1-v6 的历史表结构，并且 PromptEpoch 表在 v2-v6
都有明确的逐版本列契约。但合并前的一条分支曾把尚未建立 Transcript 表的 v6 表结构
写成 `user_version=7`；这类数据库虽然数据完整，却会被当前版本误判为
`CHECKPOINT_MIGRATION_LEGACY_TABLE_UNEXPECTED`。与此同时，真正的当前 v7+ schema
若残留 PromptEpoch，仍然不能根据版本号猜测来源。

## 当前问题

输入、判断和结果如下：

```text
本机 v3 数据库
→ 严格 schema 校验通过
→ 发现 PromptEpoch
→ 入口只接受 v6
→ Host 失败关闭，CLI 显示 THREAD_STORE_UNAVAILABLE
```

数据库没有损坏，但用户只能换用空 HOME 或手工移走历史数据库才能启动。直接删除数据库
会丢失 Thread 历史，不应作为代码缺陷的默认处理方式。

## 解决方案

1. 用两个明确谓词区分合法来源：标准 v2-v6，以及完整 v6 表集合误标为 v7 且没有
   Transcript/Context Snapshot 后继表的旧分支形状；为什么：兼容真实合并前数据库，
   同时不放宽到缺少契约的版本；验证：回归测试覆盖上下界和旧分支 v7。
2. 父进程入口和 migration child 使用同一谓词；为什么：避免父子对同一数据库得出不同结论；
   验证：合法 v3 可以进入 child，v7+ 在备份和写入前拒绝。
3. 对合法来源统一执行现有单向 adapter；为什么：PromptEpoch 只转换为 legacy snapshot，
   不恢复生产双读或双写；验证：最终 snapshot 可读、旧表删除、无可证明 Run 时不伪造绑定。
4. 保留 verified backup、事务提交和失败恢复；为什么：本地历史不可逆；验证：备份仍为原始
   schema，最终数据库完整性为 `ok`，二次打开幂等。
5. 更新用户排障和架构说明；为什么：同事需要区分可迁移旧库和异常残留表；验证：项目文档检查通过。

## 范围

- 严格合法的 v2-v6 PromptEpoch 一次性迁移。
- v3 真实历史形状的回归测试。
- v7 及更高异常残留表的失败关闭保护。

## 非范围

- 不处理 migration child terminate/kill/reap 与临时文件回收；这些仍属于 ZC-108。
- 不支持未知表、畸形列、损坏数据库或当前 schema v7+ 的 PromptEpoch adapter；只兼容
  能通过完整 v6 表契约校验的旧分支 v7 数据库。
- 不保留 PromptEpoch 生产读写、alias、fallback 或双写。
- 不修改 Protocol、CLI 或上下文压缩行为。

## 验收清单

- [x] 严格合法的 v3 数据库可以自动升级到当前 schema。
- [x] 升级前生成 `pre-v3-migration.bak`，备份保持 v3 且完整性为 `ok`。
- [x] PromptEpoch 转为 legacy RunContextSnapshot，旧表在成功事务中删除。
- [x] 缺少旧 Run binding 时不创建伪造 binding。
- [x] v7、v8、v9、v10 和当前 schema 异常残留 PromptEpoch 时原样拒绝。
- [x] 合并前旧分支 v7（无 Transcript 后继表）自动按 v6 迁移，Team 状态表和旧数据保持完整。
- [x] 迁移后的数据库可再次打开，且 Thread/Transcript 行为不回退。

## 验证命令

```bash
cd packages/agent && .venv/bin/python -m pytest -q \
  tests/threads/test_thread_persistence.py \
  tests/threads/test_context_lifecycle.py
cd ../.. && bun run project:check
bun run typecheck
bun run test
```

另使用真实 v3 数据库的临时副本执行一次迁移，只记录 schema、完整性、表和行数结果，
不读取或提交 Prompt、消息、凭据等内容。

## 版本影响

这是开发态本地数据库迁移缺陷修复，按 patch 候选记录。本任务不调用 `version:set`，
待 ZC-108 完成后再与上下文迁移可靠性改动统一评估发布版本。

## 前置

- ZC-107
