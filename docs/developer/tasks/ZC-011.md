---
id: ZC-011
title: 阻止 Shell 白名单的复合命令绕过
priority: P0
status: 已完成
owner: codex
branch: master
scope: 用可测试的 shell 词法策略替换仅检查首命令的 allowlist，并拒绝管道、重定向、命令替换和控制运算符绕过。
acceptance: 允许的单一命令保持可用；复合命令或无法可靠解析的命令在执行前 fail closed。
user_docs: docs/user/安全与沙箱.md
developer_docs: docs/developer/架构总览.md
test_evidence: 2026-07-22：Shell 词法与中间件定向回归 29 通过；Python 全量 134 通过、1 跳过；bun run protocol:check、bun run typecheck、bun run build 通过；TUI Markdown 定向 7 通过。Bun 全量仅命中既有 ZC-053 Tree-sitter 间歇抖动（83 通过、1 跳过、1 失败），与本任务 Python Shell 链路无关。版本保持 0.1.0，无版本变更。
references: ZC-053
completed_at: 2026-07-22
---

## 范围

限制 `ShellAllowListMiddleware` 的输入为可安全判断的单命令形式，并对绝对命令路径、参数和 shell 特殊语法建立明确策略。

## 非范围

不实现完整 shell 解释器、命令沙箱、网络策略或工作区文件路径边界。
不调整当前 `0.1.0` 版本号。

## 验收清单

- `allowed && forbidden`、管道、重定向、`$()`、反引号和换行拼接被拒绝。
- 白名单内的单一编译、测试和 Git 只读命令仍可执行。
- 畸形引号或无法解析的输入 fail closed，且测试不调用真实危险命令。
