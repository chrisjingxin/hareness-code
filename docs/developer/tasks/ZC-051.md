---
id: ZC-051
title: 收紧 Skill 悬浮调色板的视觉密度与层次
priority: P1
status: 已完成
owner: codex
branch: main
scope: 参照 MiMo Code DialogSelect 的层次，收紧 /skills 的卡片、搜索和列表行样式，修复空白过多与描述折行，同时保留底层会话。
acceptance: 卡片无亮色描边且不清屏；列表高度随结果数收缩；选中行高对比且描述不折行；常规和窄终端渲染回归通过。
user_docs: 不涉及
developer_docs: 不涉及
test_evidence: bun run typecheck（通过）；bun run test（TS 76 passed, 1 skipped；Python 79 passed, 1 skipped）；bun run build（通过）；git diff --check（通过）
references: ZC-050
completed_at: 2026-07-20
---

## 范围

- 使用无描边面板并保留底层会话；当前 OpenTUI 的半透明背景会覆写底层字符，不能作为 scrim 使用。
- 搜索框融入面板，移除额外边框；目录按实际结果数设置最大高度。
- 使用单行截断与高对比选中状态，保证两列列表稳定扫描。

## 非范围

- 不改变 Skill 发现、选择、执行或 Slash 命令行为。
- 不引入 MiMo Code 的运行时、主题系统或组件依赖。

## 验收清单

- OpenTUI 渲染断言覆盖背景保留、单行描述与结果数收缩。
- 类型检查、全量测试、构建和项目一致性检查通过。
