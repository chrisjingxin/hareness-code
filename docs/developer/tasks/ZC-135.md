---
id: ZC-135
title: 切换 Snapshot 约束的 canonical 文件工具契约
feature_area: Agent 文件读写可靠性
parent_task: ZC-131
decomposed_by: Codex
priority: P0
status: 待认领
owner: 未认领
branch: -
reviewed_at: 2026-08-10
review_due: 2026-08-24
scope: 根据 ZC-133 已确认的唯一 schema，在 ZC-134 接管边界上实现 canonical read_file、write_file、edit_file、delete_file：局部 read 生成 Thread Snapshot/seen ranges，write 只创建，edit/delete 强制 Snapshot 和当前内容校验，同一 edit 调用全部预检且只允许已读范围；同步迁移静态 schema、prompt、Capability、Policy、子 Agent 和测试并删除旧参数路径。
acceptance: 模型只看到 ZC-133 胜出的单一文件工具 schema；read 返回短 Snapshot、普通源行号、总行数与区间，局部 read 不把整文件送入 context；write 对已有目标零写入；edit/delete 对缺失/过期/跨 Thread/路径/backend/stale Snapshot、未读/越界/重叠范围均零写入并返回稳定 code；同批一项失败时全部零写入；虚拟文件和非文本文件不可 edit；旧 old_string/replace_all 或未胜出的 range schema 已彻底删除，无 alias/fallback。
user_docs: docs/user/交互使用.md、docs/user/故障排查.md
developer_docs: docs/developer/designs/ZC-131.md、docs/developer/architecture/架构总览.md
test_evidence: -
references: docs/developer/tasks/ZC-131.md、docs/developer/tasks/ZC-133.md、docs/developer/tasks/ZC-134.md
completed_at: -
---

## 服务的用户结果

弱模型只需要使用一套短、明确的文件工具，并且不能通过猜路径、猜行号或复用旧版本绕过
“先读当前内容再修改”的边界。

## 前置条件

- ZC-133 已把最终 edit/insert schema、默认 read limit 和输出上限写回 ZC-131 设计。
- ZC-134 已提供唯一 tool seam、ThreadSnapshotStore 和 backend adapter contract。

如果任一条件不满足，本任务不得自行补决策。

## 实施步骤

1. 实现 canonical `read_file`：读取完整内容 identity，向模型只返回请求窗口、普通源行号、
   Snapshot ID、shown/total lines；相同内容重复读取合并 seen ranges。
2. 明确空文件、超长行、offset/limit 超界、最大字节、UTF-8/BOM/CRLF/LF、末尾换行和
   multimodal/Notebook 的返回语义；视觉分段编号不得成为可编辑源行号。
3. 实现 create-only `write_file`：目标存在返回 `FILE_ALREADY_EXISTS`；成功后返回实际内容
   Snapshot 和首个局部窗口。
4. 实现评测胜出的 `edit_file` schema：校验 Snapshot scope/hash、seen range、范围、数量、
   overlap、no-op 和内容上限；所有 edit 先在内存应用，任一失败零写入。
5. 实现 `delete_file(snapshot_id)`：复用相同 scope/hash 校验并保持 DELETE 风险审批；成功后
   使该路径 Snapshot 失效。
6. 返回 ZC-131 定义的稳定 JSON error code 和唯一 `next_action`，控制消息长度并避免把源码
   内容写入日志。
7. 同步 `_BUILTIN_TOOL_SHAPES`、schema fingerprint、Capability/Policy、风险/并发分类、
   deferred tool、主/子 Agent prompt 和工具展示。
8. 删除旧 `old_string/new_string/replace_all`、未胜出的候选 schema、兼容转换和测试入口；
   当前未正式发版，不保留 legacy alias。
9. 更新交互使用和故障排查，说明 Snapshot 过期/stale 后局部重读，不暴露内部 hash。

## 明确不修改

- 不在本任务实现 diff 审批增强和本地原子 writer 细节（ZC-136），但必须继续经过现有
  Capability/Approval，不能形成未授权写入中间态。
- 不自动 formatter/LSP，不做 stale recovery。
- 不支持跨文件 batch、移动或目录删除。

## 验收清单

- [ ] 每种模型/主子 Agent 只看到一套最终 schema。
- [ ] 未读、stale、跨作用域、非法/重叠 edit 均返回正确 code 且字节不变。
- [ ] 局部 read 只进入局部 context，但完整内容 identity 正确。
- [ ] write/create、edit/modify、delete 的职责不可互换。
- [ ] 旧 schema、转换、alias 和 fallback 已删除。
- [ ] focused、runtime、policy、Thread isolation tests 通过。

## 定期复核记录

- 2026-08-10（Codex）：从 ZC-131 拆解；依赖 ZC-133/ZC-134，下一次复核 2026-08-24。

