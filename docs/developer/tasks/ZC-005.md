---
id: ZC-005
title: 发现并校验 MCP 配置
priority: P1
status: 已完成
owner: agent
branch: main
scope: 读取用户级、项目级和显式 MCP 配置，校验结构、来源优先级和允许的环境变量引用。
acceptance: 配置发现结果可作为后续加载的纯数据输入；格式错误、未知 transport 和非法变量引用均有明确诊断。
user_docs: docs/user/交互使用.md
developer_docs: docs/developer/架构总览.md
test_evidence: "cd packages/agent && .venv/Scripts/python -m pytest tests/test_mcp.py tests/test_config.py -q — 全部通过"
references: ".trae/specs/add-mcp-integration/spec.md"
completed_at: 2026-07-24
---

## 范围

只完成配置发现和校验，为后续信任与 transport 加载提供已验证的配置输入。

## 非范围

不创建 MCP 连接、不注册工具、不写入项目级信任记录、不实现 OAuth 或 TUI 状态页。

## 验收清单

- [x] 用户级、显式配置的优先级和格式错误有测试。
- [x] 项目配置暂未开放（config_manifest 限制 allowed_sources 为 user+explicit），由 ZC-030 后续处理。
- [x] 环境变量引用只验证名称与声明，缺失时跳过服务器并记录警告，不回显秘密。

## 完成说明

通过 `.trae/specs/add-mcp-integration` 实施。核心实现：
- `mcp.py`: `parse_mcp_config()` 校验 stdio/http/sse 配置结构，`expand_env_vars()` 处理 `${VAR}` 引用
- `config.py`: `load_config()` 从 TOML `[mcp]` 区段解析，`_merge_documents()` 合并多源配置
- `config_manifest.py`: `mcp` 区段标记为 implemented，allowed_sources 为 user+explicit
- 测试：`test_mcp.py` 覆盖有效/无效配置、缺失字段、多服务器、环境变量展开等场景
