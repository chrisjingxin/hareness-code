---
id: ZC-047
title: 升级企业 DeepAgents 与 MCP Adapter 依赖
priority: P0
status: 阻塞
owner: codex
branch: main
scope: 将 Agent 的 deepagents 与 langchain-mcp-adapters 升至企业已提供版本，并用企业索引重生成 uv.lock。
acceptance: pyproject.toml 与 uv.lock 固定 deepagents 0.6.12、langchain-mcp-adapters 0.2.2；Python 回归和锁文件检查通过；依赖清单同步。
user_docs: docs/user/快速开始.md
developer_docs: docs/developer/dependency-versions.json、docs/developer/dependency-inventory.json
test_evidence: 2026-07-17：uv lock --offline 无法解析 langchain-mcp-adapters==0.2.2，确认该版本不在本机缓存；当前环境未注入企业 UV 索引或凭据。已回退直接依赖声明，避免 pyproject.toml 与 uv.lock 不一致。
references: ZC-043、ZC-046
completed_at: -
---

## 范围

- 仅升级两个企业已有 Python 包，并保留其他直接依赖的既定精确版本。
- 使用环境或企业 CI 注入的索引配置更新锁文件，禁止将地址或凭据写入仓库。

## 非范围

- 不调整 Agent 行为、协议或模型配置。
- 不重新引入 QuickJS、js_eval 或 langchain-tests。

## 阻塞原因

需要通过 `UV_INDEX_URL`（及企业凭据环境变量）访问企业 Python 索引后，才能解析 0.6.12 / 0.2.2 的全部 Python 版本与平台工件，并更新 `uv.lock`。
