---
id: ZC-138
title: 支持安全初始化已有空文件
feature_area: Agent 文件读写可靠性
parent_task: -
decomposed_by: Codex
priority: P1
status: 待认领
owner: 未认领
branch: -
reviewed_at: 2026-08-11
review_due: 2026-08-25
scope: 为已经存在但内容为空的 UTF-8 文本建立唯一、可审批、受 Snapshot 和 CAS 保护的初始化路径，消除 write_file 拒绝已有目标、edit_file 又禁止空 old_string 导致的无解状态。
acceptance: 模型先读取已有空文件后可以用单一 canonical 语义写入初始内容；非空文件不得以空匹配执行隐式插入；跨 Thread、stale Snapshot、审批后竞态、超限内容和不支持 CAS 的 backend 全部零写入；成功后返回实际落盘内容的新 Snapshot 和 changed range；主 Agent、子 Agent、工具 schema、prompt、用户文档和回归测试保持一致。
user_docs: docs/user/交互使用.md、docs/user/故障排查.md
developer_docs: 待确定
test_evidence: -
references: docs/developer/tasks/ZC-131.md、docs/developer/designs/ZC-131.md
completed_at: -
---

## 背景

当前空文件可以通过 `read_file` 获得 Snapshot，但后续没有合法的文件工具路径：

- `write_file` 对已存在目标返回 `FILE_ALREADY_EXISTS`。
- `edit_file` 对空 `old_string` 返回 `INVALID_EDIT`。

这会让模型在“先读后改”和“只创建新文件”两条正确规则之间陷入无法恢复的循环。

## 用户结果

```text
读取已有空文件
  → 获得当前 Thread 的空内容 Snapshot
  → 表达初始内容
  → 展示完整 diff 并审批
  → CAS 提交
  → 返回实际新 Snapshot
```

## 需要确认的设计

- 在实施前创建同 ID Design，决定是为空文件引入受限的 edit 语义，还是引入更明确的初始化动作。
- 不得把空 `old_string` 普遍解释为文件开头、结尾或任意位置的插入。
- 弱模型可理解性发生变化时，必须复用 ZC-131 的真实 Profile 评测门槛。

## 范围

- Snapshot 文件工具 schema、prepare/approval/commit 路径与稳定错误语义。
- 空文件、批准期间外部变化、跨 Thread、超限和远端 CAS 回归。
- 模型 prompt、用户交互/故障排查和架构文档的语义同步。

## 非范围

- 不提供一般化的按行插入、批量 edits 或 `replace_all`。
- 不改变 `write_file` 的 create-only 保证。
- 不编辑 Notebook、二进制、图片或不可无损解码文件。

## 验收清单

- [ ] 已有空文件可在先读、审批和 CAS 保护下写入初始内容。
- [ ] 非空文件的空匹配、stale/跨 Thread Snapshot 和审批后竞态全部零写入。
- [ ] 成功结果使用落盘后实际内容建立 Snapshot，支持后续连续编辑。
- [ ] 主/子 Agent 只暴露一套最终语义，文档与测试同步。

## 定期复核记录

- 2026-08-11（Codex）：从 ZC-131 实际空文件无解状态拆出独立用户结果；实施前需确认同 ID Design，下次复核 2026-08-25。
