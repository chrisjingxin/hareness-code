---
id: ZC-053
title: 消除 OpenTUI Markdown 渲染测试抖动
priority: P1
status: 待认领
owner: 未认领
branch: -
scope: 固定 Bun 全量测试中 Tree-sitter client 的初始化与销毁生命周期，消除 Markdown 标题间歇丢失。
acceptance: 连续全量运行 TUI 测试不再出现 TreeSitter client destroyed，Markdown 标题和 fenced code 回归稳定通过。
user_docs: 不涉及
developer_docs: 不涉及
test_evidence: 2026-07-22：`bun run test` 两次出现 `thread 通过原生 Markdown renderer 隐藏标题和代码围栏标记` 失败与 `TreeSitter client destroyed`；同一用例定向 7/7 通过，后续全量 84 通过/1 跳过，确认为间歇生命周期抖动。
references: ZC-045、ZC-046
completed_at: -
---

## 范围

- 定位 OpenTUI 测试 renderer 共享 Tree-sitter client 被提前销毁的时序。
- 使 Markdown 渲染用例可重复、可并行地稳定运行。

## 非范围

- 不改变 Markdown 视觉、语法资源清单或产品交互。

## 验收清单

- 连续多次运行 `bun run test:ts` 与根 `bun run test` 均通过。
- 定向 Markdown 和离线语法高亮回归通过。
- `bun run typecheck`、`bun run build` 与 `bun run project:check` 通过。
